{
  "convertedModels": [
    "shift",
    "employeeAvailability",
    "overtimeAccount",
    "overtimeTransaction",
    "saldoSnapshot",
    "timeEntry",
    "break",
    "presenceDevice",
    "leaveType",
    "leaveEntitlement",
    "employeeVocationalSchoolPattern",
    "section9Credit",
    "absence",
    "leaveRequest"
  ],
  "exceptions": [
    {
      "file": "apps/api/src/contexts/platform/api/test-bootstrap.ts",
      "calls": [
        { "call": "overtimeAccount.create", "line": 172 },
        { "call": "leaveType.create", "line": 179 },
        { "call": "leaveEntitlement.create", "line": 187 },
        { "call": "employeeAvailability.deleteMany", "line": 241 },
        { "call": "employeeShiftPattern.deleteMany", "line": 244 },
        { "call": "employeeVocationalSchoolPattern.deleteMany", "line": 247 },
        { "call": "shift.deleteMany", "line": 250 },
        { "call": "absence.deleteMany", "line": 251 },
        { "call": "leaveRequest.deleteMany", "line": 252 },
        { "call": "leaveEntitlement.deleteMany", "line": 253 },
        { "call": "saldoSnapshot.deleteMany", "line": 256 },
        { "call": "timeEntry.deleteMany", "line": 257 },
        { "call": "overtimeTransaction.deleteMany", "line": 258 },
        { "call": "overtimeAccount.deleteMany", "line": 261 },
        { "call": "overtimePlan.deleteMany", "line": 264 },
        { "call": "leaveType.deleteMany", "line": 271 },
        { "call": "coverageRule.deleteMany", "line": 273 },
        { "call": "shiftTemplate.deleteMany", "line": 274 },
        { "call": "companyShutdown.deleteMany", "line": 275 },
        { "call": "terminalApiKey.deleteMany", "line": 276 },
        { "call": "terminalApiKey.create", "line": 331 }
      ],
      "reason": "D-03 (Phase 100b): this route resets a single test-only tenant's full dataset for e2e bootstrapping. The 17 deleteMany calls (lines 241-276) are the teardown: giving every context its own destructive resetForTests() on its PERMANENT public surface would be the worse trade in an audit-proof system, so this direct-access path is excepted instead of facaded. The route is gated behind ALLOW_TEST_BOOTSTRAP, never registers on int or prod, and its teardown only ever matches tenant IDs of the form ^test-[a-zA-Z0-9_-]{8}$, so the exception exists for something that is not reachable in a real environment at all. The 4 create calls (overtimeAccount:172, leaveType:179, leaveEntitlement:187, terminalApiKey:331) are seeding calls for the same test-only bootstrap route and are excepted on the same grounds; terminalApiKey.create at line 331 is a separate seeding block outside the teardown handler proper but serves the identical test-bootstrap purpose."
    },
    {
      "file": "apps/api/src/contexts/working-time-account/overtime-balance.ts",
      "calls": [
        { "call": "timeEntry.count", "line": 125 },
        { "call": "timeEntry.findMany", "line": 139 }
      ],
      "reason": "Phase 101B (Issue #101): computeOvertimeBalanceBreakdown's hasTodayEntries cutoff check and its worked-minutes-since-snapshot read were moved verbatim, unedited, out of time-tracking/api/time-entries.ts into this file, per the owner's own Nebenbefund decision that the whole overtime-balance closure belongs in working-time-account on domain grounds (101B-ZYKLEN-BEFUND.md §4) — measured cycle-neutral, not measured against this Phase-100b gate. Before the move these two calls sat IN the time-tracking area, so they were never foreign; the move itself, not a behaviour change, is what turns them foreign. `time-tracking/facade/time-entries.ts` has no existing function shaped like either query (todayEntries-count-as-cutoff-decider; date-ranged WORK-type sum), and this plan's own non-negotiable is a byte-for-byte relocation (verbatim diff proved in the plan's SUMMARY) — writing a new facade function and rewiring these two call sites is a real, valid follow-up but is a rewrite, which this plan is not authorised to do. Tracked as a residual driver alongside the ones 101B-ZYKLEN-BEFUND.md §7 already hands to ADR 0001 Eintrag H (Block-2, #102-#104)."
    }
  ]
}
