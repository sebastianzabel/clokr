node_modules
dist
build
.svelte-kit
generated
coverage
pnpm-lock.yaml

# Helm chart templates use Go template syntax ({{ }}) inside YAML —
# prettier's YAML parser chokes on them. Other templates in this dir
# (deployment-api.yaml, ingress.yaml, etc.) were committed pre-Phase-72
# without lint-staged coverage; this exclusion keeps them consistent.
charts/**/templates/*.yaml
charts/**/templates/*.tpl

# Phase 75b (Issue #75): the neutrality-matrix recording is written by the test itself, one cell
# per line in code-point key order, so a diff names exactly the cells that changed. Prettier would
# re-wrap it on commit and make the next RECORD run differ from the committed bytes.
apps/api/src/__tests__/neutrality/recorded/
