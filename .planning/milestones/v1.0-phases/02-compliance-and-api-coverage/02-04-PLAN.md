---
phase: 02-compliance-and-api-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/__tests__/leave.test.ts
  - apps/api/src/__tests__/overtime-calc.test.ts
autonomous: true
requirements: [API-02, API-03]

must_haves:
  truths:
    - "Leave request lifecycle (create, approve, reject, cancel, cancellation-approve) is tested end-to-end"
    - "Cross-year leave booking splits correctly across year boundaries"
    - "Overtime saldo read returns numeric values (workedMinutes, expectedMinutes, balanceMinutes)"
    - "Monatsabschluss (month-close) creates a SaldoSnapshot record"
    - "Entries in a closed/locked month are immutable (edit/delete rejected)"
  artifacts:
    - path: "apps/api/src/__tests__/leave.test.ts"
      provides: "Full leave lifecycle tests including cancellation flow and cross-year booking"
      contains: "COMPLIANCE"
    - path: "apps/api/src/__tests__/overtime-calc.test.ts"
      provides: "Saldo read, month-close, locked-month immutability tests"
      contains: "COMPLIANCE"
  key_links:
    - from: "apps/api/src/__tests__/leave.test.ts"
      to: "apps/api/src/routes/leave.ts"
      via: "app.inject() to POST/PUT /api/v1/leave"
      pattern: "/api/v1/leave"
    - from: "apps/api/src/__tests__/overtime-calc.test.ts"
      to: "apps/api/src/routes/overtime.ts"
      via: "app.inject() to GET/POST /api/v1/overtime"
      pattern: "/api/v1/overtime"
---

<objective>
Add comprehensive leave request lifecycle and overtime/Monatsabschluss tests.

Purpose: Leave management (API-02) involves a complex lifecycle with multiple state transitions (PENDING -> APPROVED/REJECTED, cancellation flow) that must be audit-proof. Overtime saldo and Monatsabschluss (API-03) are financially and legally critical — incorrect saldo calculation or broken month-close undermines payroll accuracy. Both require targeted compliance testing.

Output: Extended leave.test.ts with cancellation flow and cross-year tests. Extended overtime-calc.test.ts with Monatsabschluss and locked-month tests.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compliance-and-api-coverage/02-RESEARCH.md
@.planning/phases/02-compliance-and-api-coverage/02-CONTEXT.md
@.planning/codebase/TESTING.md

@apps/api/src/__tests__/leave.test.ts
@apps/api/src/__tests__/overtime-calc.test.ts
@apps/api/src/__tests__/setup.ts
@apps/api/src/routes/leave.ts
@apps/api/src/routes/overtime.ts

<interfaces>
<!-- Leave lifecycle endpoints (from CLAUDE.md) -->
POST /api/v1/leave — create leave request (status: PENDING)
PUT /api/v1/leave/:id/approve — approve (PENDING -> APPROVED)
PUT /api/v1/leave/:id/reject — reject (PENDING -> REJECTED)
PUT /api/v1/leave/:id/cancel — request cancellation (APPROVED -> CANCELLATION_REQUESTED)
PUT /api/v1/leave/:id/approve-cancellation — approve cancellation (CANCELLATION_REQUESTED -> CANCELLED)

<!-- Overtime/saldo endpoints -->
GET /api/v1/overtime/:employeeId — read saldo (workedMinutes, expectedMinutes, balanceMinutes)
POST /api/v1/overtime/close-month — trigger Monatsabschluss (creates SaldoSnapshot, sets isLocked on entries)

<!-- Prisma models -->
SaldoSnapshot: { id, employeeId, periodType, periodStart, periodEnd, workedMinutes, expectedMinutes, balanceMinutes, carryOver, closedAt, closedBy }

From setup.ts:
- seedTestData returns: { ..., leaveType, leaveEntitlement }
- Suffix "lv" used by leave.test.ts, "ot" used by overtime-calc.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add leave lifecycle compliance tests including cancellation and cross-year</name>
  <files>apps/api/src/__tests__/leave.test.ts</files>
  <read_first>
    apps/api/src/__tests__/leave.test.ts
    apps/api/src/__tests__/setup.ts
    apps/api/src/routes/leave.ts (search for "cancel", "CANCELLATION_REQUESTED", "approve-cancel", "cross-year" to understand endpoint URLs and status transitions)
    CLAUDE.md (Leave Cancellation Flow section)
  </read_first>
  <behavior>
    - Test: POST /api/v1/leave creates request with status PENDING -> 201
    - Test: PUT approve changes status to APPROVED -> 200
    - Test: PUT reject changes status to REJECTED -> 200
    - Test: PUT cancel on APPROVED request changes to CANCELLATION_REQUESTED -> 200
    - Test: PUT approve-cancellation changes to CANCELLED -> 200
    - Test: cross-year booking (Dec 30 - Jan 5) splits days correctly across years
    - Test: cancellation by the same manager who approved is blocked (self-approval blocked per CLAUDE.md)
  </behavior>
  <action>
    Per D-05, extend existing leave.test.ts with compliance describe blocks.

    **Add describe block "COMPLIANCE: Leave cancellation lifecycle"** containing:

    1. "creates a leave request with PENDING status" — POST /api/v1/leave with:
       ```
       { employeeId: data.employee.id, leaveTypeId: data.leaveType.id, startDate: "2025-06-02", endDate: "2025-06-06", days: 5, note: "Urlaub" }
       ```
       Assert: 201, body.status === "PENDING" (or similar field name — read the route first)

    2. "admin approves leave request" — PUT to approve endpoint with leaveId. Assert: 200, status APPROVED.

    3. "admin requests cancellation of approved leave" — PUT to cancel endpoint. Assert: 200, status CANCELLATION_REQUESTED.

    4. "another manager approves cancellation" — Per CLAUDE.md: cancellation requires approval by a DIFFERENT manager. If seedTestData does not provide a manager user, create one inline. PUT to approve-cancellation endpoint. Assert: 200, status CANCELLED.

    5. "rejects self-approval of cancellation" — If admin approved the original request, admin cannot approve the cancellation. Test that attempting this returns 403 or similar rejection.

    **Add describe block "COMPLIANCE: Cross-year leave booking"** containing:

    6. "splits cross-year booking across Dec and Jan" — POST /api/v1/leave with startDate "2025-12-29" (Mon) and endDate "2026-01-02" (Fri). The executor must read the route to confirm how cross-year booking works. Assert: response indicates days are split (or separate requests created), OR verify directly via Prisma that entitlement deduction spans both years. This may need adjustment based on what the route actually does.

    **NOTE:** The exact endpoint URLs, payload fields, and response structure MUST be confirmed by reading leave.ts first. The patterns above are educated guesses from CLAUDE.md and RESEARCH.md. The executor must adapt.

    Per D-01: if any test reveals a bug (e.g., cancellation self-approval is not blocked), fix the route inline and open a GitHub issue per D-02.
  </action>
  <verify>
    <automated>docker compose exec api pnpm vitest run src/__tests__/leave.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - leave.test.ts contains describe block with text "cancellation" or "Cancellation"
    - leave.test.ts has test asserting POST creates leave with PENDING status
    - leave.test.ts has test asserting approve changes status to APPROVED
    - leave.test.ts has test asserting cancellation request changes status to CANCELLATION_REQUESTED
    - leave.test.ts has test asserting cancellation approval changes status to CANCELLED
    - leave.test.ts contains describe block with text "cross-year" or "Cross-year"
    - `docker compose exec api pnpm vitest run src/__tests__/leave.test.ts` exits 0
  </acceptance_criteria>
  <done>Leave lifecycle fully tested: PENDING -> APPROVED -> CANCELLATION_REQUESTED -> CANCELLED. Self-approval of cancellation blocked. Cross-year booking verified. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add overtime saldo and Monatsabschluss compliance tests</name>
  <files>apps/api/src/__tests__/overtime-calc.test.ts</files>
  <read_first>
    apps/api/src/__tests__/overtime-calc.test.ts
    apps/api/src/__tests__/saldo-snapshot.test.ts
    apps/api/src/__tests__/setup.ts
    apps/api/src/routes/overtime.ts (search for "close", "lock", "snapshot", "SaldoSnapshot" to understand month-close endpoint)
    packages/db/prisma/schema.prisma (SaldoSnapshot model)
  </read_first>
  <behavior>
    - Test: GET /api/v1/overtime/:employeeId returns saldo with numeric fields
    - Test: month-close trigger creates a SaldoSnapshot record
    - Test: after month-close, time entries in that month have isLocked = true
    - Test: editing an entry in a closed month is rejected
  </behavior>
  <action>
    Per D-05, extend existing overtime-calc.test.ts with compliance describe blocks.

    **Add describe block "COMPLIANCE: Overtime saldo read"** containing:

    1. "returns saldo with workedMinutes and expectedMinutes" — First ensure the employee has at least one time entry in the past (create via Prisma if not already present from seed). GET /api/v1/overtime/{data.employee.id} with adminToken. Assert: 200, body contains numeric workedMinutes (or equivalent field — read the route first), expectedMinutes, balanceMinutes.

    **Add describe block "COMPLIANCE: Monatsabschluss (month-close)"** containing:

    2. "month-close creates a SaldoSnapshot" — First create a time entry for a specific month (e.g., 2025-02-10). Then trigger month-close for that month via the appropriate endpoint (read overtime.ts to find it — could be POST /api/v1/overtime/close-month or similar). Assert: a SaldoSnapshot record exists for that employee and month via `app.prisma.saldoSnapshot.findFirst({ where: { employeeId, periodStart: ..., periodEnd: ... } })`. Assert: snapshot has workedMinutes > 0.

    3. "month-close sets isLocked on entries" — After month-close, query the time entries for that month: `app.prisma.timeEntry.findMany({ where: { employeeId, date: { gte: monthStart, lte: monthEnd } } })`. Assert: all entries have isLocked === true.

    4. "rejects edit of entry in closed month" — After month-close, attempt PUT on one of the locked entries. Assert: statusCode is 403, 409, or 422.

    **IMPORTANT:** The executor MUST read overtime.ts to discover the exact month-close endpoint URL, required payload, and response structure. The saldo-snapshot.test.ts file may also provide patterns. If month-close requires admin role, use adminToken. If it requires specific year/month parameters, provide them.

    Use a month in the past (e.g., February 2025) that won't conflict with other tests. Clean up SaldoSnapshot and related entries in afterAll.
  </action>
  <verify>
    <automated>docker compose exec api pnpm vitest run src/__tests__/overtime-calc.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - overtime-calc.test.ts contains describe block with text "saldo" or "Saldo"
    - overtime-calc.test.ts contains describe block with text "Monatsabschluss" or "month-close"
    - overtime-calc.test.ts has test asserting GET overtime endpoint returns numeric fields
    - overtime-calc.test.ts has test that queries SaldoSnapshot after month-close and asserts it exists
    - overtime-calc.test.ts has test asserting isLocked is true on entries after month-close
    - overtime-calc.test.ts has test asserting PUT on locked entry is rejected (403, 409, or 422)
    - `docker compose exec api pnpm vitest run src/__tests__/overtime-calc.test.ts` exits 0
  </acceptance_criteria>
  <done>Overtime saldo read returns numeric fields. Monatsabschluss creates SaldoSnapshot and locks entries. Locked entries reject edit attempts. All tests pass.</done>
</task>

</tasks>

<verification>
Run both test files:
```bash
docker compose exec api pnpm vitest run src/__tests__/leave.test.ts src/__tests__/overtime-calc.test.ts --reporter=verbose
```
Then full suite for regression check:
```bash
docker compose exec api pnpm vitest run --reporter=verbose 2>&1 | tail -20
```
</verification>

<success_criteria>
1. Leave lifecycle fully tested: create, approve, reject, cancel, cancellation-approve with correct status transitions
2. Cross-year leave booking tested (Dec-Jan split)
3. Overtime saldo read returns numeric fields
4. Monatsabschluss creates SaldoSnapshot and sets isLocked on entries
5. Locked-month entries reject edits
6. All tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-compliance-and-api-coverage/02-04-SUMMARY.md`
</output>
