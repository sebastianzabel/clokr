---
phase: 02-compliance-and-api-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/__tests__/time-entries.test.ts
autonomous: true
requirements: [API-01, SEC-04]

must_haves:
  truths:
    - "Time entry CRUD operations (create, edit, delete) are tested with valid data returning correct status codes"
    - "DELETE on time entry sets deletedAt instead of hard-deleting the row"
    - "GET queries filter out soft-deleted entries (deletedAt not null)"
    - "Editing or deleting a time entry in a locked month returns 403 or 409"
    - "Creating a duplicate time entry for the same employee+date returns 409"
  artifacts:
    - path: "apps/api/src/__tests__/time-entries.test.ts"
      provides: "CRUD tests, soft-delete tests, locked-month immutability tests, duplicate-day rejection"
      contains: "COMPLIANCE: Soft delete"
  key_links:
    - from: "apps/api/src/__tests__/time-entries.test.ts"
      to: "apps/api/src/routes/time-entries.ts"
      via: "app.inject() to POST/PUT/DELETE /api/v1/time-entries"
      pattern: "time-entries"
---

<objective>
Add comprehensive time entry CRUD, soft-delete enforcement, and locked-month immutability tests.

Purpose: Time entries are the core data entity. API-01 requires full CRUD test coverage including edge cases (duplicate-day 409, locked-month rejection). SEC-04 requires verification that soft deletes work correctly and locked months are truly immutable. These tests prove the audit-proof data integrity of the system.

Output: Extended time-entries.test.ts with compliance describe blocks for soft-delete, locked-month, and duplicate-day behavior.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compliance-and-api-coverage/02-RESEARCH.md
@.planning/phases/02-compliance-and-api-coverage/02-CONTEXT.md
@.planning/codebase/TESTING.md

@apps/api/src/__tests__/time-entries.test.ts
@apps/api/src/__tests__/setup.ts
@apps/api/src/routes/time-entries.ts

<interfaces>
<!-- Key types and endpoints -->
From apps/api/src/routes/time-entries.ts:
- POST /api/v1/time-entries — create entry (payload: { employeeId, date, startTime, endTime, breakMinutes, note? })
- PUT /api/v1/time-entries/:id — update entry
- DELETE /api/v1/time-entries/:id — soft delete (sets deletedAt)
- GET /api/v1/time-entries?employeeId=&month=&year= — list entries (filters deletedAt: null)

From Prisma schema:
- TimeEntry.isLocked: Boolean @default(false) — set after Monatsabschluss
- TimeEntry.deletedAt: DateTime? — soft delete timestamp
- SaldoSnapshot model exists (not MonthlyPeriod) — executor must verify how isLocked is set

From apps/api/src/__tests__/setup.ts:
- seedTestData returns: { tenant, adminUser, adminToken, empUser, empToken, employee, workSchedule, ... }
- Suffix "te" already used by time-entries.test.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add time entry CRUD coverage and duplicate-day 409 test</name>
  <files>apps/api/src/__tests__/time-entries.test.ts</files>
  <read_first>
    apps/api/src/__tests__/time-entries.test.ts
    apps/api/src/__tests__/setup.ts
    apps/api/src/routes/time-entries.ts (search for "409" and "already exists" to confirm duplicate-day logic)
  </read_first>
  <behavior>
    - Test: POST /api/v1/time-entries with valid data returns 201 and entry object with id, employeeId, date, startTime, endTime
    - Test: PUT /api/v1/time-entries/:id with valid data returns 200 and updated entry
    - Test: DELETE /api/v1/time-entries/:id returns 200 or 204
    - Test: POST for same employee+date when entry already exists returns 409
    - Test: GET /api/v1/time-entries with employeeId filter returns array of entries
  </behavior>
  <action>
    Per D-05, extend the existing time-entries.test.ts with new describe blocks. Do NOT create a separate file.

    **Add describe block "COMPLIANCE: Time entry CRUD completeness"** inside the existing top-level describe, containing:

    1. "creates a time entry with valid data" — POST /api/v1/time-entries with payload:
       ```
       { employeeId: data.employee.id, date: "2025-04-07", startTime: "2025-04-07T07:00:00Z", endTime: "2025-04-07T15:30:00Z", breakMinutes: 30 }
       ```
       Assert: statusCode 201, body has entry.id, entry.employeeId equals data.employee.id. Clean up after test.

    2. "updates an existing time entry" — First create entry for 2025-04-08, then PUT with updated breakMinutes: 45. Assert: statusCode 200, body shows breakMinutes: 45.

    3. "deletes a time entry via API" — Create entry for 2025-04-09, DELETE it. Assert: statusCode 200 or 204.

    4. "rejects duplicate entry for same employee and date with 409" — Create entry for 2025-04-10, then POST again for same date. Assert: statusCode 409. Body should contain an error message.

    5. "lists time entries with employeeId filter" — Create entry for 2025-04-11, GET /api/v1/time-entries?employeeId={id}&year=2025&month=4. Assert: 200, body contains array with at least 1 entry matching the employeeId.

    Use unique dates (2025-04-07 through 2025-04-11, all Mondays-Fridays) to avoid conflicts with other tests. Clean up entries in afterAll or within each test using app.prisma.timeEntry.deleteMany where date matches.
  </action>
  <verify>
    <automated>docker compose exec api pnpm vitest run src/__tests__/time-entries.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - time-entries.test.ts contains describe block with text "CRUD" or "completeness"
    - time-entries.test.ts has test asserting POST returns 201
    - time-entries.test.ts has test asserting PUT returns 200 with updated field
    - time-entries.test.ts has test asserting DELETE returns 200 or 204
    - time-entries.test.ts has test asserting duplicate POST returns 409
    - `docker compose exec api pnpm vitest run src/__tests__/time-entries.test.ts` exits 0
  </acceptance_criteria>
  <done>Time entry CRUD fully tested: create (201), update (200), delete (200/204), duplicate-day rejection (409), list with filter (200). All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add soft-delete enforcement and locked-month immutability tests</name>
  <files>apps/api/src/__tests__/time-entries.test.ts</files>
  <read_first>
    apps/api/src/__tests__/time-entries.test.ts
    apps/api/src/routes/time-entries.ts (search for "isLocked", "deletedAt", and "locked" to understand enforcement logic)
    packages/db/prisma/schema.prisma (search for "isLocked" and "SaldoSnapshot" to confirm model structure)
  </read_first>
  <behavior>
    - Test: DELETE sets deletedAt timestamp, does NOT remove the row from DB
    - Test: GET /api/v1/time-entries excludes entries where deletedAt is not null
    - Test: PUT on time entry where isLocked=true returns 403 or 409
    - Test: DELETE on time entry where isLocked=true returns 403 or 409
  </behavior>
  <action>
    Per D-05, extend existing time-entries.test.ts. Per SEC-04, verify soft-delete and locked-month immutability.

    **Add describe block "COMPLIANCE: Soft delete enforcement"** containing:

    1. "DELETE sets deletedAt, does not hard-delete" — Per RESEARCH.md code example:
       - Create entry for 2025-05-05 via POST
       - DELETE via API
       - Query directly: `app.prisma.timeEntry.findUnique({ where: { id: entryId } })`
       - Assert: row is NOT null (still exists)
       - Assert: row.deletedAt is NOT null (soft-deleted)

    2. "GET excludes soft-deleted entries" —
       - Create entry for 2025-05-06 via Prisma directly
       - Soft-delete it: `app.prisma.timeEntry.update({ where: { id }, data: { deletedAt: new Date() } })`
       - GET /api/v1/time-entries?employeeId={id}&year=2025&month=5
       - Assert: response array does NOT contain the soft-deleted entry's id

    **Add describe block "COMPLIANCE: Locked month immutability"** containing:

    3. "rejects PUT on entry in locked month" —
       - Create entry for 2025-01-15 directly via Prisma with isLocked: true
       - Alternatively: first find how isLocked is set. Read the time-entries route to understand if isLocked is on the TimeEntry itself or checked via a SaldoSnapshot/MonthlyClose record. Implement accordingly.
       - PUT /api/v1/time-entries/:id with breakMinutes: 45
       - Assert: statusCode is 403, 409, or 422 (any rejection is acceptable)

    4. "rejects DELETE on entry in locked month" —
       - Same setup as above (locked entry)
       - DELETE /api/v1/time-entries/:id
       - Assert: statusCode is 403, 409, or 422

    **IMPORTANT:** Read the route handler to determine HOW locked-month is implemented. The Prisma schema shows `isLocked` directly on TimeEntry. But there may also be a SaldoSnapshot-based check. The executor MUST read the route code first to understand the locking mechanism before writing tests. If the route checks `isLocked` on the TimeEntry model, set it directly. If it checks a separate table (SaldoSnapshot with closedAt), use that instead.

    Clean up all test entries in afterAll to prevent pollution.
  </action>
  <verify>
    <automated>docker compose exec api pnpm vitest run src/__tests__/time-entries.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - time-entries.test.ts contains describe block with text "Soft delete" or "soft-delete"
    - time-entries.test.ts contains describe block with text "Locked month" or "locked"
    - time-entries.test.ts has test that queries Prisma directly after DELETE and asserts deletedAt is not null
    - time-entries.test.ts has test that asserts GET excludes soft-deleted entries
    - time-entries.test.ts has test asserting PUT on locked entry returns 403, 409, or 422
    - time-entries.test.ts has test asserting DELETE on locked entry returns 403, 409, or 422
    - `docker compose exec api pnpm vitest run src/__tests__/time-entries.test.ts` exits 0
  </acceptance_criteria>
  <done>Soft delete verified: DELETE sets deletedAt (row persists), GET filters deletedAt:null. Locked-month immutability verified: PUT and DELETE on locked entries rejected. All tests pass.</done>
</task>

</tasks>

<verification>
Run time-entries test suite:
```bash
docker compose exec api pnpm vitest run src/__tests__/time-entries.test.ts --reporter=verbose
```
All existing AND new tests pass. Then run full suite to confirm no regressions:
```bash
docker compose exec api pnpm vitest run --reporter=verbose 2>&1 | tail -20
```
</verification>

<success_criteria>
1. Time entry CRUD fully covered: create 201, update 200, delete 200/204, duplicate 409, list 200
2. Soft delete proven: row persists with deletedAt set after API DELETE
3. Soft delete filtering proven: GET excludes entries with non-null deletedAt
4. Locked-month immutability proven: PUT and DELETE on locked entries rejected
5. All tests pass with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-compliance-and-api-coverage/02-03-SUMMARY.md`
</output>
