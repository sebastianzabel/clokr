---
phase: 03-e2e-and-ui-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/leave-flow.spec.ts
  - apps/e2e/tests/admin-settings-flow.spec.ts
autonomous: true
requirements: [E2E-03, E2E-04, E2E-05, UI-05]

must_haves:
  truths:
    - "Leave request creation submits successfully and the request appears in the Meine Antraege list"
    - "Leave request approval by admin changes the request status to APPROVED in the UI"
    - "Admin can create a new employee via the UI form and the employee appears in the employee table"
    - "Monatsabschluss close-month action clicks the close button after seeding a closeable month, and the UI reflects the closed status"
    - "Password policy settings can be changed and saved, and the changes persist after page reload"
  artifacts:
    - path: "apps/e2e/tests/leave-flow.spec.ts"
      provides: "E2E-03 leave request and approval flow"
      contains: "APPROVED"
    - path: "apps/e2e/tests/admin-settings-flow.spec.ts"
      provides: "E2E-04 employee management, E2E-05 Monatsabschluss, UI-05 password policy"
      contains: "Monatsabschluss"
  key_links:
    - from: "apps/e2e/tests/leave-flow.spec.ts"
      to: "/api/v1/leave"
      via: "Employee-authenticated page.request.post for leave creation, admin page.request.put for approval"
      pattern: "api/v1/leave"
    - from: "apps/e2e/tests/admin-settings-flow.spec.ts"
      to: "/api/v1/overtime/close-month"
      via: "Pre-seed time entry via API, then UI button click triggering close-month API"
      pattern: "close-month"
    - from: "apps/e2e/tests/admin-settings-flow.spec.ts"
      to: "/api/v1/settings/security"
      via: "Password policy save calls PUT endpoint"
      pattern: "settings/security"
---

<objective>
Write substantive E2E tests for the leave approval flow (E2E-03), admin employee management (E2E-04), Monatsabschluss lock/unlock (E2E-05), and password policy persistence (UI-05).

Purpose: Replace scaffold tests that only navigate to pages with real tests that exercise state changes, API interactions, and persistence.

Output: Updated `leave-flow.spec.ts` with leave creation and approval assertions, updated `admin-settings-flow.spec.ts` with employee creation, Monatsabschluss close, and password policy save tests.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-e2e-and-ui-quality/03-CONTEXT.md
@.planning/phases/03-e2e-and-ui-quality/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/e2e/tests/helpers.ts:
```typescript
export const TEST_ADMIN = {
  email: process.env.TEST_ADMIN_EMAIL || "admin@clokr.de",
  password: process.env.TEST_ADMIN_PASSWORD || "Admin123!Pass",
};
export async function loginAsAdmin(page: Page): Promise<void>;
export async function screenshotPage(page: Page, name: string): Promise<void>;
```

Seeded users (from packages/db/src/seed.ts):
- admin@clokr.de (ADMIN role) - password: admin1234 (from .env: TEST_ADMIN_PASSWORD=admin1234)
- max@clokr.de (EMPLOYEE role) - password: mitarbeiter5678

Leave API:
- POST /api/v1/auth/login - { email, password } -> { accessToken, refreshToken, user }
- POST /api/v1/leave - create leave request (body: { employeeId, type, startDate, endDate, ... })
- PUT /api/v1/leave/:id - update leave request
- PUT /api/v1/leave/:id/review - approve/reject (body: { action: "APPROVED" | "REJECTED" })
- Self-approval blocked: "Eigene Antraege koennen nicht selbst genehmigt werden" (403)
- Self-approval check (leave.ts line 502): compares reviewer's employeeId with request's employeeId

SvelteKit proxy (hooks.server.ts line 26):
- Forwards ALL headers including Authorization to Fastify backend
- Explicit Authorization header in page.request calls will be forwarded correctly

Employee API:
- POST /api/v1/employees - create employee
- GET /api/v1/employees - list employees

Monatsabschluss:
- POST /api/v1/overtime/close-month - { employeeId, year, month }
- GET /api/v1/overtime/close-month/year-status?year=YYYY - returns months[].status
- Frontend closeMonth() iterates readyEmployees, calls POST per employee
- UI success: "abgeschlossen" text appears, month badge changes

Time entry API (for Monatsabschluss pre-seeding):
- POST /api/v1/time-entries - { employeeId, date, startTime, endTime }
- A past-month time entry is required for an employee to appear in "ready" state

Password policy:
- PUT /api/v1/settings/security - { passwordMinLength, passwordRequireUpper, ... }
- GET /api/v1/settings/security - returns current settings
- Frontend: savePasswordPolicy() in admin/system/+page.svelte (line ~462)
- UI fields: pwMinLength input, pwRequireUpper/Special toggle switches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write substantive leave approval E2E flow</name>
  <read_first>
    - apps/e2e/tests/leave-flow.spec.ts
    - apps/e2e/tests/helpers.ts
    - apps/web/src/routes/(app)/leave/+page.svelte (form structure, tab names, approval UI)
    - apps/api/src/routes/leave.ts (self-approval check at line ~502, review endpoint)
    - packages/db/src/seed.ts (seeded employee data: max@clokr.de password is "mitarbeiter5678")
  </read_first>
  <files>apps/e2e/tests/leave-flow.spec.ts</files>
  <action>
    Per D-01, write substantive test bodies in the EXISTING file. Per D-03, exercise the employee-creates -> manager-approves path. The self-approval check (leave.ts line 502) compares the reviewer's employeeId with the leave request's employeeId -- they must be different.

    Replace the file body with substantive tests. Keep the describe wrapper "Abwesenheiten - Complete Flow" and beforeEach (loginAsAdmin, goto /leave, waitForLoadState).

    **Strategy for leave approval (E2E-03) avoiding self-approval block:**

    The admin user cannot approve their own leave request (API returns 403 "Eigene Antraege koennen nicht selbst genehmigt werden"). To guarantee the employee-creates -> admin-approves path works without any self-approval ambiguity:

    1. **Authenticate as employee via API:** Call `page.request.post("/api/v1/auth/login", { data: { email: "max@clokr.de", password: "mitarbeiter5678" } })` to get an employee `accessToken`. This login endpoint does not require auth, so the admin browser context cookies are irrelevant.
    2. **Get the employee ID:** Use the admin context `page.request.get("/api/v1/employees")` and find `employees.find((e: any) => e.user?.email === "max@clokr.de")` to get the employee record with its `id`.
    3. **Create leave request AS the employee:** `page.request.post("/api/v1/leave", { headers: { Authorization: \`Bearer ${employeeToken}\` }, data: { employeeId: maxEmployee.id, type: "SICK", startDate: futureDate, endDate: futureDate, note: "E2E test leave" } })`. Using the employee's own token ensures the request is owned by the employee, not the admin. Use SICK type to avoid vacation entitlement constraints.
    4. Capture the leave ID from the response JSON: `const leaveData = await leaveRes.json(); const leaveId = leaveData.id;`
    5. **Approve AS admin:** Use `page.request.put(\`/api/v1/leave/${leaveId}/review\`, { data: { action: "APPROVED" } })` -- this uses the admin's browser context auth (no explicit header override). Since the admin is NOT the request owner (max@clokr.de owns it), the self-approval block does not apply.
    6. Navigate to the Genehmigungen tab in the UI and assert the approved leave is visible.

    **No conditional fallback paths.** This strategy is deterministic -- employee creates, admin approves, no self-approval concern.

    **Test structure:**

    Test "page loads with tabs and vacation summary" - keep as-is (uses hard expects already).

    Test "create leave request via UI form":
    1. Click "Neuer Antrag"
    2. `await expect(page.locator("#f-type").first()).toBeVisible();` (hard assert, not optional)
    3. Select leave type: `await page.locator("#f-type").first().selectOption("SICK");` (SICK avoids entitlement issues)
    4. Set start date to 14 days from now (weekday): compute and fill `#f-start`
    5. Set end date to same date: fill `#f-end`
    6. Click submit: `await page.getByRole("button", { name: /einreichen|antrag stellen/i }).first().click();`
    7. Wait for success: `await page.waitForLoadState("networkidle");`
    8. Assert: navigate to "Meine Antraege" tab, verify the request appears
    9. `await screenshotPage(page, "flow-leave-created");`

    Test "approve leave request (employee-creates, admin-approves)":
    1. Login as employee via API: `const loginRes = await page.request.post("/api/v1/auth/login", { data: { email: "max@clokr.de", password: "mitarbeiter5678" } }); const { accessToken: employeeToken } = await loginRes.json();`
    2. Get employee list via admin context: `const empRes = await page.request.get("/api/v1/employees"); const employees = await empRes.json(); const maxEmployee = employees.find((e: any) => e.user?.email === "max@clokr.de");`
    3. Compute a future weekday date (30 days out)
    4. Create a SICK leave request AS the employee: `const leaveRes = await page.request.post("/api/v1/leave", { headers: { Authorization: \`Bearer ${employeeToken}\` }, data: { employeeId: maxEmployee.id, type: "SICK", startDate, endDate } }); const leaveData = await leaveRes.json(); const leaveId = leaveData.id;`
    5. Approve AS admin (uses browser context auth, no header override): `const approveRes = await page.request.put(\`/api/v1/leave/${leaveId}/review\`, { data: { action: "APPROVED" } }); expect(approveRes.ok()).toBeTruthy();`
    6. Navigate to the leave page and click the "Genehmigungen" tab
    7. Assert the approved leave is visible in the UI (text "Genehmigt" or status indicator)
    8. Alternatively, verify via API: `const statusRes = await page.request.get(\`/api/v1/leave/${leaveId}\`); expect((await statusRes.json()).status).toBe("APPROVED");`
    9. `await screenshotPage(page, "flow-leave-approved");`

    Test "special leave shows reason dropdown" - keep existing test but convert optional assertions to hard assertions.

    **Patterns:**
    - All `if (await el.isVisible())` -> `await expect(el).toBeVisible()`
    - Use `page.request` for API calls within E2E tests (inherits browser context auth)
    - Use explicit `headers: { Authorization: \`Bearer ${token}\` }` to override auth for employee-context API calls
    - Use `screenshotPage()` after each test
    - Use SICK leave type to avoid vacation entitlement constraints in test data
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "await expect" apps/e2e/tests/leave-flow.spec.ts && grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts && grep -c "mitarbeiter5678\|max@clokr" apps/e2e/tests/leave-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await expect" apps/e2e/tests/leave-flow.spec.ts` outputs at least `6`
    - `grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts` outputs at least `3` (login as employee, create leave as employee, approve as admin)
    - `grep "mitarbeiter5678\|max@clokr" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1` (employee credentials used)
    - `grep "Authorization.*Bearer\|headers.*Authorization" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1` (explicit employee auth header)
    - `grep "APPROVED\|Genehmigt\|genehmig" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
    - No `if (await` optional assertion pattern remains in the file
    - No conditional fallback path (no `if.*fail\|if.*error.*fall` pattern for leave creation)
    - File still imports `loginAsAdmin` and `screenshotPage` from `"./helpers"`
  </acceptance_criteria>
  <done>leave-flow.spec.ts has substantive leave creation and approval tests with hard assertions. The approval flow uses employee credentials (max@clokr.de / mitarbeiter5678) to create the leave request via API with explicit Authorization header, then admin approves via browser context auth -- deterministic employee-creates -> admin-approves path without any self-approval concern or conditional fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Write substantive admin employee, Monatsabschluss, and password policy E2E tests</name>
  <read_first>
    - apps/e2e/tests/admin-settings-flow.spec.ts
    - apps/e2e/tests/helpers.ts
    - apps/web/src/routes/(app)/admin/employees/+page.svelte (employee form fields, table structure)
    - apps/web/src/routes/(app)/admin/monatsabschluss/+page.svelte (closeMonth function, status display, lines 162-213)
    - apps/web/src/routes/(app)/admin/system/+page.svelte (savePasswordPolicy function, security section, lines 462-481)
    - apps/api/src/routes/settings.ts (GET/PUT /settings/security endpoint, Zod schema)
    - apps/api/src/routes/time-entries.ts (POST /time-entries schema for seeding)
  </read_first>
  <files>apps/e2e/tests/admin-settings-flow.spec.ts</files>
  <action>
    Per D-01, write substantive test bodies in the EXISTING file. Per D-04, Monatsabschluss must click close-month and assert isLocked. Per D-11/D-12, password policy UI is already built -- only E2E test needed.

    Keep the describe wrapper "Admin Settings - Complete Flow" and beforeEach (loginAsAdmin).

    Keep existing tests that already have value: "navigate all admin tabs", "admin special-leave - view rules table", "admin special-leave - open create modal". Convert any `if (await el.isVisible())` optional patterns to hard `await expect(el).toBeVisible()`.

    **Add/replace these tests:**

    Test "admin employees - create new employee" (E2E-04):
    1. `await page.goto("/admin/employees"); await page.waitForLoadState("networkidle");`
    2. Click "Mitarbeiter anlegen": `await page.getByText(/Mitarbeiter anlegen/i).first().click();`
    3. Assert modal opens: `const modal = page.locator("[role='dialog'], .modal").first(); await expect(modal).toBeVisible();`
    4. Fill the form fields with unique test data:
       - firstName: `await modal.getByLabel(/Vorname/i).fill("E2E");`
       - lastName: `await modal.getByLabel(/Nachname/i).fill("Testmitarbeiter");`
       - email: `await modal.getByLabel(/E-Mail/i).fill(\`e2e-${Date.now()}@test.de\`);`
       - employeeNumber: `await modal.getByLabel(/Personalnummer/i).fill(\`E2E-${Date.now()}\`);`
       - If a hire date field exists: fill with today's date
    5. Click save/create: `await modal.getByRole("button", { name: /anlegen|erstellen|speichern/i }).first().click();`
    6. Wait for response: `await page.waitForLoadState("networkidle");`
    7. Assert modal closes and employee appears in table: `await expect(page.getByText("E2E Testmitarbeiter").first()).toBeVisible({ timeout: 5_000 });`
    8. `await screenshotPage(page, "flow-admin-employee-created");`

    Test "admin monatsabschluss - seed closeable month, click close, and assert locked" (E2E-05, per D-04):

    **CRITICAL: This test MUST pre-seed data to guarantee a closeable month exists. Do NOT use conditional fallbacks that skip the close action.**

    Pre-seed step (before navigating to Monatsabschluss page):
    1. Get the employee list via API: `const empRes = await page.request.get("/api/v1/employees"); const employees = await empRes.json();`
    2. Pick an employee (preferably the seeded max@clokr.de employee): `const targetEmployee = employees.find((e: any) => e.user?.email === "max@clokr.de") || employees[0];`
    3. Determine a past month that is NOT already closed. Use a month 2-3 months ago (e.g., January 2026). Check status via API: `const yearRes = await page.request.get("/api/v1/overtime/close-month/year-status?year=2026"); const yearData = await yearRes.json();`
    4. Find an open month from the year-status response: `const openMonth = yearData.months?.find((m: any) => m.status !== "closed");` If all months are closed, try a different year.
    5. Create a time entry for that employee in the target month via API: `await page.request.post("/api/v1/time-entries", { data: { employeeId: targetEmployee.id, date: "2026-01-15", startTime: "2026-01-15T08:00:00.000Z", endTime: "2026-01-15T16:30:00.000Z" } });` Adjust the date to fall within the identified open month. If the POST returns 409 (entry already exists for that date), the employee already has data -- proceed.

    Close-month UI test (after seeding):
    6. `await page.goto("/admin/monatsabschluss"); await page.waitForLoadState("networkidle");`
    7. Assert page title: `await expect(page.getByText(/Monatsabschluss/).first()).toBeVisible();`
    8. Assert months are listed: `await expect(page.getByText(/Januar|Februar|März/).first()).toBeVisible();`
    9. Navigate to the target month's year if needed (use year navigation arrows in the UI)
    10. Find the close button for the target month. Look for a button associated with the month name (e.g., "Januar") that triggers the close action. This might be a "Monat abschliessen" button or an icon button near the month row.
    11. Click the close button: `await closeBtn.click();`
    12. Wait for the API call to complete: `await page.waitForLoadState("networkidle");`
    13. Assert the UI reflects the closed status: `await expect(page.getByText(/abgeschlossen/i).first()).toBeVisible({ timeout: 10_000 });`
    14. Verify via API that the month is actually closed: `const verifyRes = await page.request.get("/api/v1/overtime/close-month/year-status?year=2026"); const verifyData = await verifyRes.json(); const closedMonth = verifyData.months?.find((m: any) => m.month === targetMonthNumber); expect(closedMonth?.status).toBe("closed");`
    15. `await screenshotPage(page, "flow-admin-monatsabschluss-closed");`

    Test "admin system - save password policy and verify persistence" (UI-05, per D-11):
    1. `await page.goto("/admin/system"); await page.waitForLoadState("networkidle");`
    2. Scroll to the "Passwort-Richtlinie" section: `await page.getByText("Passwort-Richtlinie").scrollIntoViewIfNeeded();`
    3. Read the current min-length value: `const minLengthInput = page.locator("input[type='number']").filter({ has: page.locator("..").filter({ hasText: /Mindestlange|mindestlange/i }) }).first();` or find it by nearby label. Alternative: use `page.locator("#pw-min-length")` if the input has an ID.
    4. Get the current value, change it: store current value, clear input, type a new value (e.g., if current is 8, change to 12)
    5. Find and click the save button for the password policy section: `await page.getByRole("button", { name: /speichern/i }).filter({ has: page.locator("..").filter({ hasText: /Passwort/i }) }).first().click();` or more directly find the save button near the password policy section
    6. Wait for save: `await page.waitForLoadState("networkidle");`
    7. Reload the page to verify persistence: `await page.reload(); await page.waitForLoadState("networkidle");`
    8. Scroll to the password policy section again
    9. Assert the saved value persists: read the min-length input value and assert it equals the value set in step 4
    10. Restore the original value: change back to original, click save
    11. `await screenshotPage(page, "flow-admin-password-policy-saved");`

    **Patterns:**
    - All `if (await el.isVisible())` -> `await expect(el).toBeVisible()`
    - Use `page.request` for API pre-seeding and verification calls
    - Use `screenshotPage()` after each test
    - Unique test data (Date.now() suffix) to avoid collisions
    - NEVER use conditional fallbacks that skip the close-month action (per D-04)
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "await expect" apps/e2e/tests/admin-settings-flow.spec.ts && grep -c "Mitarbeiter anlegen" apps/e2e/tests/admin-settings-flow.spec.ts && grep -c "Passwort" apps/e2e/tests/admin-settings-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await expect" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `10`
    - `grep "E2E.*Testmitarbeiter\|e2e-.*@test.de" apps/e2e/tests/admin-settings-flow.spec.ts | wc -l` outputs at least `1` (employee creation test data)
    - `grep -c "close-month\|Monatsabschluss\|abgeschlossen" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `2`
    - `grep -c "page.request.post" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1` (pre-seed time entry via API)
    - `grep -Ec "\.click\(\)|page\.request\.post.*close-month" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `3` (close button click + other clicks are present; E2E-05 must include a click action, not just navigation)
    - `grep -c "Passwort-Richtlinie\|password.*policy\|settings/security" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
    - `grep -c "page.reload" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1` (persistence verification)
    - No `if (await` optional assertion pattern remains in the file
    - No conditional fallback that skips close-month click (no `if.*closeable\|if.*ready.*navigate` pattern)
    - File still imports `loginAsAdmin` and `screenshotPage` from `"./helpers"`
  </acceptance_criteria>
  <done>admin-settings-flow.spec.ts has substantive employee creation (E2E-04), Monatsabschluss close with pre-seeded data and click+API verification (E2E-05), and password policy save+persistence (UI-05) tests. The Monatsabschluss test pre-seeds a time entry via API to guarantee a closeable month, clicks the close button, and asserts locked status via both UI text and API verification. All use hard assertions, no conditional fallbacks.</done>
</task>

</tasks>

<verification>
- `grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts` outputs at least `3`
- `grep "APPROVED\|Genehmigt" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
- `grep "mitarbeiter5678\|max@clokr" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
- `grep "Authorization.*Bearer" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
- `grep -c "E2E" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
- `grep -c "page.reload" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
- `grep -c "page.request.post" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1` (pre-seed for Monatsabschluss)
- `grep -rn "if (await.*isVisible" apps/e2e/tests/leave-flow.spec.ts apps/e2e/tests/admin-settings-flow.spec.ts` outputs no matches
</verification>

<success_criteria>
1. leave-flow.spec.ts contains substantive leave creation and approval flow tests with hard assertions (E2E-03)
2. Approval flow authenticates as employee (max@clokr.de) via API login, creates leave with employee token, and approves with admin session -- no self-approval ambiguity
3. No conditional fallback paths in the approval test
4. admin-settings-flow.spec.ts contains employee creation test with form fill and table assertion (E2E-04)
5. admin-settings-flow.spec.ts contains Monatsabschluss test that pre-seeds a closeable month via API, clicks the close button, and asserts locked status via UI and API verification (E2E-05, per D-04)
6. admin-settings-flow.spec.ts contains password policy save and persistence test with page reload (UI-05, per D-11)
7. No `if (await el.isVisible())` optional assertion patterns remain in modified files
8. No conditional fallbacks that skip the close-month action
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-and-ui-quality/03-02-SUMMARY.md`
</output>
