---
phase: 01-test-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - eslint.config.js
autonomous: true
requirements:
  - TEST-04
must_haves:
  truths:
    - "Any unhandled Promise in TypeScript files causes pnpm lint to exit non-zero with an error (not a warning)"
    - "Type-aware linting is active (parserOptions.project: true wired)"
  artifacts:
    - path: "eslint.config.js"
      provides: "Type-aware rule block with no-floating-promises as error"
      contains: "no-floating-promises"
  key_links:
    - from: "eslint.config.js"
      to: "TypeScript project"
      via: "parserOptions.project: true"
      pattern: "parserOptions"
---

<objective>
Enable `@typescript-eslint/no-floating-promises` as a blocking ESLint error with type-aware linting active.

Purpose: After Plan 01 eliminates all `.catch(() => {})` patterns, this rule will enforce that new async code is always handled. Without `parserOptions.project`, the rule fires silently — this plan adds both the type-aware config and the rule.
Output: eslint.config.js with a new typed-rules block; `pnpm lint` fails on any future unhandled Promise.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@.planning/phases/01-test-infrastructure/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Current eslint.config.js structure. Extracted from codebase. -->
The current config uses ESLint flat config format (array export). Structure:
1. `eslint.configs.recommended`
2. `...tseslint.configs.recommended`
3. `...sveltePlugin.configs["flat/recommended"]`
4. `eslintConfigPrettier`
5. Global languageOptions (browser + node globals)
6. Svelte-specific block (files: ["**/*.svelte"])
7. Ignores block
8. General rules block (no-explicit-any warn, no-unused-vars warn, no-console warn, no-empty warn)

The TypeScript parser is NOT currently configured with `parserOptions.project`. Type-aware rules will silently fail without it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add parserOptions.project and no-floating-promises to eslint.config.js</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (read full file — understand all existing blocks before inserting the new one)
  </read_first>
  <action>
    Add a new configuration block to the exported array in eslint.config.js. Insert it BEFORE the general rules block (the last array entry with `@typescript-eslint/no-explicit-any` etc.) and AFTER the ignores block:

    ```javascript
    // Type-aware rules — requires parserOptions.project
    {
      files: ["**/*.ts"],
      languageOptions: {
        parserOptions: {
          project: true,
          tsconfigRootDir: import.meta.dirname,
        },
      },
      rules: {
        "@typescript-eslint/no-floating-promises": "error",
        "@typescript-eslint/no-misused-promises": "error",
      },
    },
    ```

    Important: `files: ["**/*.ts"]` scopes these rules to TypeScript files only. The `parserOptions.project: true` auto-discovers the nearest tsconfig.json for each file. `import.meta.dirname` resolves to the directory containing eslint.config.js (the monorepo root).

    Do NOT apply this block to `**/*.svelte` files — the Svelte parser handles those separately and `no-floating-promises` does not apply cleanly to Svelte component event handlers.

    After inserting the block, verify the config exports a valid array by checking that the file parses without syntax errors.

  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep "no-floating-promises" eslint.config.js` returns 1 match with value `"error"`
    - `grep "no-misused-promises" eslint.config.js` returns 1 match with value `"error"`
    - `grep "parserOptions" eslint.config.js` returns at least 1 match
    - `grep "project: true" eslint.config.js` returns 1 match
    - `grep "tsconfigRootDir" eslint.config.js` returns 1 match
    - `grep 'files.*\*\*\/\*\.ts' eslint.config.js` returns 1 match (the new block's scope)
    - `pnpm --filter @clokr/api lint` exits 0 (no new violations — all .catch() patterns already removed by Plan 01)
    - `pnpm lint` exits 0 across the monorepo
  </acceptance_criteria>
  <done>eslint.config.js has type-aware TypeScript block with no-floating-promises as error; lint passes cleanly after Plan 01's catch removals.</done>
</task>

</tasks>

<verification>
1. `grep "no-floating-promises" eslint.config.js` — must return `"error"` (not `"warn"`)
2. `grep "project: true" eslint.config.js` — must return 1 match
3. `pnpm lint` — must exit 0 (all .catch() patterns were removed in Plan 01)
4. Create a temporary test file with `async function test() { Promise.resolve(); }` and verify `pnpm lint` reports an error on it, then delete it.
</verification>

<success_criteria>

- eslint.config.js contains type-aware block scoped to `**/*.ts`
- `@typescript-eslint/no-floating-promises` is set to `"error"`
- `@typescript-eslint/no-misused-promises` is set to `"error"`
- `parserOptions.project: true` and `tsconfigRootDir: import.meta.dirname` are set
- `pnpm lint` exits 0 across the monorepo (no pre-existing violations)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-04-SUMMARY.md`
</output>
