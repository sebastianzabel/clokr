---
phase: 01-test-infrastructure
plan: 06
type: execute
wave: 3
depends_on:
  - 01-05
files_modified:
  - apps/api/vitest.config.ts
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "pnpm --filter @clokr/api test:coverage fails if lines/functions/branches drop below the enforced thresholds"
    - "Thresholds are set at or slightly below the measured baseline (not aspirational targets)"
    - "pnpm --filter @clokr/api test:coverage currently passes (no pre-existing threshold breach)"
  artifacts:
    - path: "apps/api/vitest.config.ts"
      provides: "coverage.thresholds block with lines, functions, branches values"
      contains: "thresholds"
  key_links:
    - from: "apps/api/vitest.config.ts coverage.thresholds"
      to: "vitest run --coverage exit code"
      via: "vitest threshold enforcement"
      pattern: "thresholds"
---

<objective>
Measure the current test coverage baseline and enforce thresholds slightly below it in vitest.config.ts.

Purpose: Coverage thresholds ensure test quality does not silently regress. Per D-04: measure baseline first, then set thresholds 2-5 percentage points below — do NOT set aspirational targets that break CI immediately.
Output: vitest.config.ts with `coverage.thresholds` set to realistic enforced values; `test:coverage` fails if coverage drops below them.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
@.planning/phases/01-test-infrastructure/01-RESEARCH.md
@.planning/phases/01-test-infrastructure/01-05-SUMMARY.md
</context>

<interfaces>
<!-- Current vitest.config.ts coverage block (after Plan 05 modifications). -->
```typescript
coverage: {
  provider: "v8",
  include: ["**/*.ts"],
  exclude: ["**/*.test.ts", "**/index.ts"],
  // thresholds: NOT YET SET — to be added by this plan after baseline measurement
},
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure baseline coverage and set thresholds</name>
  <files>apps/api/vitest.config.ts</files>
  <read_first>
    - apps/api/vitest.config.ts (read full file — understand current coverage config before adding thresholds)
    - apps/api/package.json (confirm test:coverage script exists: "vitest run --coverage")
  </read_first>
  <action>
    Step 1 — Measure baseline:
    Run the coverage command against the test schema (Plan 05 must be complete first):
    ```bash
    pnpm --filter @clokr/api test:coverage 2>&1 | tail -20
    ```

    Read the coverage summary table output. The v8 provider outputs a table like:
    ```
    % Coverage report from v8
    File          | % Stmts | % Branch | % Funcs | % Lines
    ...
    All files     |   XX.XX |    XX.XX |   XX.XX |  XX.XX
    ```

    Step 2 — Set thresholds 2-5 percentage points below each metric (round down to nearest whole number):
    Example: if baseline shows lines=72.3%, functions=68.1%, branches=61.4% → set lines=68, functions=64, branches=57.

    Add the thresholds to apps/api/vitest.config.ts `coverage` block:
    ```typescript
    thresholds: {
      lines: <baseline_lines - 4, rounded down>,
      functions: <baseline_functions - 4, rounded down>,
      branches: <baseline_branches - 4, rounded down>,
    },
    ```

    Use the actual measured values. Do NOT use the example numbers above unless the baseline actually produces those values.

    Step 3 — Verify threshold enforcement works:
    Run `pnpm --filter @clokr/api test:coverage` — it must exit 0 (current coverage is above thresholds).

    Step 4 — Document the baseline in the SUMMARY.md:
    Record the exact measured values and the chosen thresholds so future changes have a reference point.

  </action>
  <verify>
    <automated>pnpm --filter @clokr/api test:coverage 2>&1 | grep -E "(All files|Coverage)" | tail -5; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "thresholds" apps/api/vitest.config.ts` returns 1 match (the thresholds block)
    - `grep "lines:" apps/api/vitest.config.ts` returns 1 match inside the thresholds block
    - `grep "functions:" apps/api/vitest.config.ts` returns 1 match inside the thresholds block
    - `grep "branches:" apps/api/vitest.config.ts` returns 1 match inside the thresholds block
    - All threshold values are positive integers greater than 0
    - `pnpm --filter @clokr/api test:coverage` exits 0 (current coverage meets thresholds)
    - A quick sanity check: temporarily set thresholds to 99 (above current) → test:coverage must exit non-zero. Revert to correct values after verifying.
  </acceptance_criteria>
  <done>vitest.config.ts has coverage.thresholds set to values slightly below baseline; pnpm test:coverage passes and would fail if coverage drops.</done>
</task>

</tasks>

<verification>
1. `grep -A4 "thresholds" apps/api/vitest.config.ts` — must show lines, functions, branches with numeric values
2. `pnpm --filter @clokr/api test:coverage` — must exit 0
3. Sanity check: set lines threshold temporarily to 99, run test:coverage — must exit non-zero, then restore correct value
</verification>

<success_criteria>

- coverage.thresholds block exists in vitest.config.ts with lines, functions, branches values
- All threshold values are set below the measured baseline (D-04: not aspirational)
- `pnpm --filter @clokr/api test:coverage` exits 0
- Documented baseline and threshold values in SUMMARY.md
  </success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-06-SUMMARY.md`
</output>
