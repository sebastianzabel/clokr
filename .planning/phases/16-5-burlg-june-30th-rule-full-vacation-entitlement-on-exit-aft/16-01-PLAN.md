---
phase: 16-5-burlg-june-30th-rule-full-vacation-entitlement-on-exit-aft
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/utils/vacation-calc.ts
  - apps/api/src/utils/__tests__/vacation-calc.test.ts
autonomous: true
requirements:
  - BURLG-H2-01
  - BURLG-H2-02

must_haves:
  truths:
    - "calculateProRataVacation returns baseDays (full entitlement) when exitDate.getMonth() >= 6 AND exitDate.getFullYear() === year"
    - "calculateProRataVacation returns 15 (pro-rata) when exitDate is Jun 30 (month index 5) — existing test still passes"
    - "H2 check fires BEFORE the monthly-loop, so July 1 bypasses the loop entirely"
    - "All pre-existing tests in vacation-calc.test.ts pass without modification"
  artifacts:
    - path: "apps/api/src/utils/vacation-calc.ts"
      provides: "calculateProRataVacation with H2 short-circuit guard"
      contains: "exitDate.getMonth() >= 6"
    - path: "apps/api/src/utils/__tests__/vacation-calc.test.ts"
      provides: "H2 test coverage"
      exports: []
  key_links:
    - from: "apps/api/src/utils/__tests__/vacation-calc.test.ts"
      to: "apps/api/src/utils/vacation-calc.ts"
      via: "vitest import"
      pattern: "from.*vacation-calc"
---

<objective>
Add the § 5 Abs. 2 BUrlG "Halbjahresregel" to `calculateProRataVacation`: employees exiting in H2 (July 1+) receive the full annual entitlement, not pro-rata. Uses TDD cycle — failing tests first, then implementation.

Purpose: This is the core logic change. All downstream callers (leave booking, entitlements endpoint, approval warning) inherit the fix automatically once this function is correct.
Output: Modified `vacation-calc.ts` with H2 branch; extended test suite covering H2 boundary cases.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-5-burlg-june-30th-rule-full-vacation-entitlement-on-exit-aft/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add H2 branch to calculateProRataVacation (TDD)</name>
  <read_first>
    - apps/api/src/utils/vacation-calc.ts (current implementation — read before touching)
    - apps/api/src/utils/__tests__/vacation-calc.test.ts (existing test suite — must remain green)
  </read_first>
  <files>apps/api/src/utils/vacation-calc.ts, apps/api/src/utils/__tests__/vacation-calc.test.ts</files>
  <behavior>
    RED phase — write failing tests first in `vacation-calc.test.ts` inside the existing `describe("calculateProRataVacation")` block:

    - Test: "returns baseDays when exitDate is Jul 1 (H2, month index 6)" → `calculateProRataVacation(30, 2026, new Date(2026, 6, 1))` must equal `30`
    - Test: "returns baseDays when exitDate is Dec 31 of the same year (H2 covered by both existing test and new guard)" → already passing, keep as-is
    - Test: "returns baseDays when exitDate is Aug 15 (H2, month index 7)" → `calculateProRataVacation(30, 2026, new Date(2026, 7, 15))` must equal `30`
    - Test: "returns baseDays when exitDate is Jul 1 with non-30 base (base=25)" → `calculateProRataVacation(25, 2026, new Date(2026, 6, 1))` must equal `25`
    - Test: "still returns 15 when exitDate is Jun 30 (H1, month index 5)" → `calculateProRataVacation(30, 2026, new Date(2026, 5, 30))` must equal `15` (existing test, must remain passing)

    GREEN phase — implement in `vacation-calc.ts`:

    In `calculateProRataVacation`, after the `if (exitYear < year) return 0;` guard (line 125) and before the monthly-loop, insert:

    ```typescript
    // § 5 Abs. 2 BUrlG: exits in H2 (July 1+) receive full annual entitlement
    if (exitDate.getMonth() >= 6) return baseDays;
    ```

    The check `exitDate.getFullYear() === year` is already guaranteed by the guards above (exitYear === year at this point). No change to the existing monthly-loop below this guard.

    Check order after implementation (per D-04):
    1. `if (!Number.isFinite(baseDays) || baseDays <= 0) return 0;` (line 117)
    2. `if (exitYear > year) return baseDays;` (line 122)
    3. `if (exitYear < year) return 0;` (line 125)
    4. NEW: `if (exitDate.getMonth() >= 6) return baseDays;` (insert here)
    5. Monthly-loop (lines 128-136)
  </behavior>
  <action>
    STEP 1 (RED): Add the following test cases to the `describe("calculateProRataVacation")` block in `vacation-calc.test.ts` immediately after the existing `"returns 15 when exitDate is Jun 30"` test:

    ```typescript
    it("returns baseDays when exitDate is Jul 1 (H2 — § 5 Abs. 2 BUrlG)", () => {
      // July = month index 6 → H2 → full entitlement, no pro-rata
      expect(calculateProRataVacation(30, YEAR, new Date(YEAR, 6, 1))).toBe(30);
    });

    it("returns baseDays when exitDate is Aug 15 (H2)", () => {
      expect(calculateProRataVacation(30, YEAR, new Date(YEAR, 7, 15))).toBe(30);
    });

    it("returns baseDays for non-30 base on H2 exit (base=25, Jul 1)", () => {
      expect(calculateProRataVacation(25, YEAR, new Date(YEAR, 6, 1))).toBe(25);
    });
    ```

    Run tests — they MUST fail (RED): `cd /Users/sebastianzabel/git/clokr && pnpm --filter @clokr/api test -- --reporter=verbose 2>&1 | grep -A 3 "vacation-calc"`

    STEP 2 (GREEN): In `vacation-calc.ts`, after line 125 (`if (exitYear < year) return 0;`) and before the `let monthsWorked = 0;` line, insert exactly:

    ```typescript
    // § 5 Abs. 2 BUrlG: Beschäftigung in der zweiten Jahreshälfte → voller Urlaubsanspruch
    if (exitDate.getMonth() >= 6) return baseDays;
    ```

    STEP 3 (VERIFY): Run tests — ALL must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && pnpm --filter @clokr/api test -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|vacation-calc)"</automated>
  </verify>
  <done>
    - vacation-calc.ts contains the line: `if (exitDate.getMonth() >= 6) return baseDays;`
    - vacation-calc.test.ts contains `"returns baseDays when exitDate is Jul 1 (H2 — § 5 Abs. 2 BUrlG)"`
    - vacation-calc.test.ts contains `"returns baseDays when exitDate is Aug 15 (H2)"`
    - vacation-calc.test.ts contains `"returns baseDays for non-30 base on H2 exit"`
    - Existing test `"returns 15 when exitDate is Jun 30"` still passes
    - All tests in the file pass (0 failures)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal pure function | `calculateProRataVacation` takes only typed primitives (number, Date) — no external input crosses this boundary directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | vacation-calc.ts H2 guard | accept | Pure function with no I/O; inputs validated by callers in leave.ts. Guard is a read-only computation. |
| T-16-01-02 | Information Disclosure | test file | accept | Test file contains no secrets or PII; uses only synthetic Date objects with fixed years. |
</threat_model>

<verification>
- `pnpm --filter @clokr/api test` passes with 0 failures
- `grep -n "getMonth() >= 6" apps/api/src/utils/vacation-calc.ts` returns a match
- `grep -c "H2" apps/api/src/utils/__tests__/vacation-calc.test.ts` returns >= 2
</verification>

<success_criteria>
- `calculateProRataVacation(30, 2026, new Date(2026, 6, 1))` returns `30` (not 17.5)
- `calculateProRataVacation(30, 2026, new Date(2026, 5, 30))` returns `15` (unchanged)
- All existing tests in vacation-calc.test.ts remain green
- New H2 tests are green
</success_criteria>

<output>
After completion, create `.planning/phases/16-5-burlg-june-30th-rule-full-vacation-entitlement-on-exit-aft/16-01-SUMMARY.md`
</output>
