---
phase: 01-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/middleware/auth.ts
  - apps/api/src/routes/overtime.ts
  - apps/api/src/routes/time-entries.ts
  - apps/api/src/routes/terminals.ts
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - "No .catch(() => {}) pattern exists anywhere in apps/api/src"
    - "Every previously-silent failure site logs via app.log.error with err object and context message"
  artifacts:
    - path: "apps/api/src/middleware/auth.ts"
      provides: "API key lastUsedAt error logging"
      contains: "app.log.error"
    - path: "apps/api/src/routes/overtime.ts"
      provides: "Overtime account recalculation error logging"
      contains: "app.log.error"
    - path: "apps/api/src/routes/time-entries.ts"
      provides: "NFC card lastUsedAt error logging"
      contains: "app.log.error"
    - path: "apps/api/src/routes/terminals.ts"
      provides: "Terminal API key lastUsedAt error logging"
      contains: "app.log.error"
  key_links:
    - from: "apps/api/src/middleware/auth.ts"
      to: "app.log.error"
      via: "catch handler"
      pattern: "app\\.log\\.error"
---

<objective>
Replace all 4 silent `.catch(() => {})` patterns in the API with logged error handlers using `app.log.error`.

Purpose: Silent failures prevent diagnosis of production issues (audit trail gaps, invisible DB errors). This is a prerequisite for enabling `no-floating-promises` as a lint error (Plan 04).
Output: 4 modified files, zero silent catch blocks, ESLint-compatible promise handling.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace silent catches in auth.ts and overtime.ts</name>
  <files>apps/api/src/middleware/auth.ts, apps/api/src/routes/overtime.ts</files>
  <read_first>
    - apps/api/src/middleware/auth.ts (read full file — understand the async chain at line 42 before modifying)
    - apps/api/src/routes/overtime.ts (read full file — understand the async chain at line 36 before modifying)
  </read_first>
  <action>
    In apps/api/src/middleware/auth.ts at line 42:
    Replace: `.catch(() => {});`
    With: `.catch((err) => app.log.error({ err }, "Failed to update API key lastUsedAt"));`

    In apps/api/src/routes/overtime.ts at line 36:
    Replace: `await updateOvertimeAccount(app, employeeId).catch(() => {});`
    With: `await updateOvertimeAccount(app, employeeId).catch((err) => app.log.error({ err }, "Failed to update overtime account"));`

    Both replacements preserve fire-and-forget semantics — the outer request still succeeds. The catch body adds `app.log.error` with structured logging per D-06.

  </action>
  <verify>
    <automated>grep -n "\.catch(() => {})" /Users/sebastianzabel/git/clokr/apps/api/src/middleware/auth.ts /Users/sebastianzabel/git/clokr/apps/api/src/routes/overtime.ts; echo "Exit $? (0 = no silent catches remain)"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `.catch(() => {})` in auth.ts returns no matches
    - grep for `.catch(() => {})` in overtime.ts returns no matches
    - grep for `app.log.error` in auth.ts returns at least 1 match at the former catch site
    - grep for `app.log.error` in overtime.ts returns at least 1 match at the former catch site
    - `pnpm --filter @clokr/api typecheck` exits 0
  </acceptance_criteria>
  <done>Both files have no silent catch blocks; errors are logged with structured context.</done>
</task>

<task type="auto">
  <name>Task 2: Replace silent catches in time-entries.ts and terminals.ts</name>
  <files>apps/api/src/routes/time-entries.ts, apps/api/src/routes/terminals.ts</files>
  <read_first>
    - apps/api/src/routes/time-entries.ts (read lines 190-210 — understand the async chain around line 200)
    - apps/api/src/routes/terminals.ts (read lines 100-120 — understand the async chain around line 110)
  </read_first>
  <action>
    In apps/api/src/routes/time-entries.ts at line 200:
    Replace: `.catch(() => {});`
    With: `.catch((err) => app.log.error({ err }, "Failed to update NFC card lastUsedAt"));`

    In apps/api/src/routes/terminals.ts at line 110:
    Replace: `.catch(() => {});`
    With: `.catch((err) => app.log.error({ err }, "Failed to update terminal API key lastUsedAt"));`

    After making both replacements, run a full grep across all of apps/api/src to confirm zero silent catches remain:
    `grep -rn "\.catch(() => {})" apps/api/src/`

    If any additional silent catch patterns are found beyond these 4 known sites, apply the same fix pattern (`app.log.error({ err }, "<context message>")`).

  </action>
  <verify>
    <automated>grep -rn "\.catch(() => {})" /Users/sebastianzabel/git/clokr/apps/api/src/; echo "Exit $? (1 = no matches = all fixed)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn ".catch(() => {})" apps/api/src/` returns no output (exit code 1)
    - `grep -rn "app.log.error" apps/api/src/routes/time-entries.ts` returns at least 1 match
    - `grep -rn "app.log.error" apps/api/src/routes/terminals.ts` returns at least 1 match
    - `pnpm --filter @clokr/api typecheck` exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>All 4 (or more) silent catch blocks replaced; codebase has zero `.catch(() => {})` patterns; TypeScript still compiles.</done>
</task>

</tasks>

<verification>
Run after both tasks:
1. `grep -rn ".catch(() => {})" apps/api/src/` — must return no output
2. `pnpm --filter @clokr/api typecheck` — must exit 0
3. `pnpm --filter @clokr/api test` — existing tests must still pass (no regressions)
</verification>

<success_criteria>

- Zero `.catch(() => {})` occurrences in apps/api/src
- All 4 confirmed locations have `app.log.error({ err }, "...")` replacements
- TypeScript compiles without errors
- Existing test suite passes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure/01-01-SUMMARY.md`
</output>
