---
phase: 25-wifi-presence-stempel-fritzbox
plan: 06
type: execute
wave: 3
depends_on: ["25-04"]
files_modified:
  - apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte
  - apps/web/src/lib/api/presence.ts
  - apps/web/src/routes/(app)/admin/+layout.svelte
autonomous: true
requirements: [WIFI-02, WIFI-05]

must_haves:
  truths:
    - "Admin can create a PresenceSource key and see the raw key once in an alert"
    - "Admin can revoke a PresenceSource key via inline confirmation"
    - "Admin can see the FritzBox device list (MAC, hostname, last-seen, online/offline) and assign an employee to a device"
    - "Admin can see all employees who have opted in to WiFi presence detection"
    - "Admin navigation includes a 'WiFi-Präsenz' tab visible to ADMIN role"
  artifacts:
    - path: "apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte"
      provides: "Three-block admin UI: PresenceSource keys, FritzBox devices, opt-in overview"
      min_lines: 250
    - path: "apps/web/src/lib/api/presence.ts"
      provides: "Typed API client helpers for all presence endpoints"
      exports: ["listSources", "createSource", "revokeSource", "listDevices", "mapDevice", "listOptedInEmployees"]
  key_links:
    - from: "apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte"
      to: "/api/v1/admin/presence-sources"
      via: "api.get / api.post / api.delete calls via presence.ts helpers"
      pattern: "listSources|createSource|revokeSource"
    - from: "apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte"
      to: "/api/v1/admin/presence-sources/:id/devices"
      via: "listDevices helper"
      pattern: "listDevices"
    - from: "apps/web/src/routes/(app)/admin/+layout.svelte"
      to: "/admin/wifi-presence"
      via: "tabs array entry"
      pattern: "wifi-presence"
---

<objective>
Create the Admin WiFi-Presence page (`/admin/wifi-presence/`) with three card blocks per UI-SPEC.md, a typed API client module for all presence endpoints, and a "WiFi-Präsenz" tab in the Admin navigation.

Purpose: Gives admins full control over PresenceSource API keys (create/revoke), the live FritzBox device list with MAC→employee assignment, and a read-only opt-in overview. This surfaces the WiFi presence feature to admins after the backend (plans 25-01 through 25-04) is in place.

Output:
- `apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte` — full three-block admin page
- `apps/web/src/lib/api/presence.ts` — typed API client helpers
- `apps/web/src/routes/(app)/admin/+layout.svelte` — updated with "WiFi-Präsenz" tab (ADMIN-only)
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-wifi-presence-stempel-fritzbox/25-CONTEXT.md
@.planning/phases/25-wifi-presence-stempel-fritzbox/25-UI-SPEC.md
@.planning/phases/25-wifi-presence-stempel-fritzbox/25-RESEARCH.md

<interfaces>
<!-- Admin layout tabs array — extend with WiFi-Präsenz tab (ADMIN only). -->
<!-- Source: apps/web/src/routes/(app)/admin/+layout.svelte -->
```typescript
// Current tabs (line 24-34):
const tabs = $derived([
  { href: "/admin/employees",       label: "Mitarbeiter",     show: true },
  { href: "/admin/vacation",        label: "Urlaub & Zeiten", show: true },
  { href: "/admin/shifts",          label: "Schichtplan",     show: isManager },
  { href: "/admin/special-leave",   label: "Sonderurlaub",    show: true },
  { href: "/admin/shutdowns",       label: "Betriebsurlaub",  show: true },
  { href: "/admin/monatsabschluss", label: "Monatsabschluss", show: isManager },
  { href: "/admin/system",          label: "System",          show: true },
  { href: "/admin/import",          label: "Import",          show: isAdmin },
  { href: "/admin/audit",           label: "Audit Log",       show: isAdmin },
].filter((t) => t.show));

// Add after "Audit Log":
{ href: "/admin/wifi-presence", label: "WiFi-Präsenz", show: isAdmin },
```

<!-- API client pattern — direct api.get/post/delete calls. -->
<!-- Source: apps/web/src/lib/api/client.ts (single export) -->
```typescript
import { api } from "$api/client";
// Usage:
const result = await api.get<T>("/some/path");
const result = await api.post<T>("/some/path", body);
await api.delete("/some/path");
```

<!-- NFC Terminal key pattern (exact UI reference for Block 1). -->
<!-- Source: apps/web/src/routes/(app)/admin/system/+page.svelte lines 1280-1368 -->
// New key reveal: .alert.alert-success with <code> + "Kopieren" btn-sm btn-ghost + "Schließen" btn-sm btn-ghost
// Create form: input.form-input (flex:1) + btn-primary inline in display:flex gap:0.5rem div
// Table: .data-table with thead/tbody, class:row-revoked={key.revokedAt}, badge-green/badge-red
// Revoke: btn-sm btn-ghost text-red, no confirmation modal (inline pattern)

<!-- Pagination component -->
```typescript
import Pagination from "$lib/components/ui/Pagination.svelte";
// Usage: <Pagination total={items.length} bind:page={page} bind:pageSize={pageSize} />
```

<!-- Svelte 5 runes: $state, $derived, onMount -->
```typescript
let items = $state<Item[]>([]);
let page = $state(1);
let pageSize = $state(10);
let paged = $derived(items.slice((page - 1) * pageSize, page * pageSize));
```

<!-- Backend API endpoints (from plan 25-03 — must exist before this plan executes) -->
// GET  /api/v1/admin/presence-sources           → { sources: PresenceSource[] }
// POST /api/v1/admin/presence-sources           → { rawKey: string, source: PresenceSource }
// DELETE /api/v1/admin/presence-sources/:id     → 204 (sets revokedAt)
// GET  /api/v1/admin/presence-sources/:id/devices → { devices: FritzDevice[] }
// POST /api/v1/admin/presence-sources/:id/devices/:mac/assign → { employeeId: string }
// GET  /api/v1/admin/presence-sources/opted-in  → { employees: OptedInEmployee[] }

<!-- PresenceSource type (mirrors TerminalKey from system page) -->
```typescript
interface PresenceSource {
  id: string;
  name: string;
  keyPrefix: string;
  adapterUrl: string | null;
  lastUsedAt: string | null;
  revokedAt: string | null;
  createdAt: string;
}

interface FritzDevice {
  mac: string;
  hostname: string;
  lastSeen: string | null;  // ISO timestamp
  online: boolean;
  assignedEmployeeId: string | null;
  assignedEmployeeName: string | null;
}

interface OptedInEmployee {
  id: string;
  firstName: string;
  lastName: string;
  wifiPresenceEnabled: boolean;
  wifiOptInAt: string | null;
  wifiMacs: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed presence API client module</name>
  <files>apps/web/src/lib/api/presence.ts</files>
  <action>
Create `apps/web/src/lib/api/presence.ts` with six named export functions. All functions use the `api` client from `$api/client`. Types are defined locally in this file (no shared package yet — the types package will be updated in a later cleanup).

```typescript
import { api } from "$api/client";

export interface PresenceSource {
  id: string;
  name: string;
  keyPrefix: string;
  adapterUrl: string | null;
  lastUsedAt: string | null;
  revokedAt: string | null;
  createdAt: string;
}

export interface FritzDevice {
  mac: string;
  hostname: string;
  lastSeen: string | null;
  online: boolean;
  assignedEmployeeId: string | null;
  assignedEmployeeName: string | null;
}

export interface OptedInEmployee {
  id: string;
  firstName: string;
  lastName: string;
  wifiPresenceEnabled: boolean;
  wifiOptInAt: string | null;
  wifiMacs: string[];
}

export async function listSources(): Promise<PresenceSource[]> {
  const res = await api.get<{ sources: PresenceSource[] }>("/admin/presence-sources");
  return res.sources;
}

export async function createSource(
  name: string,
  adapterUrl?: string,
): Promise<{ rawKey: string; source: PresenceSource }> {
  return api.post<{ rawKey: string; source: PresenceSource }>("/admin/presence-sources", {
    name,
    adapterUrl: adapterUrl || undefined,
  });
}

export async function revokeSource(id: string): Promise<void> {
  await api.delete(`/admin/presence-sources/${id}`);
}

export async function listDevices(sourceId: string): Promise<FritzDevice[]> {
  const res = await api.get<{ devices: FritzDevice[] }>(
    `/admin/presence-sources/${sourceId}/devices`,
  );
  return res.devices;
}

export async function mapDevice(
  sourceId: string,
  mac: string,
  employeeId: string,
): Promise<void> {
  await api.post(`/admin/presence-sources/${sourceId}/devices/${encodeURIComponent(mac)}/assign`, {
    employeeId,
  });
}

export async function listOptedInEmployees(): Promise<OptedInEmployee[]> {
  const res = await api.get<{ employees: OptedInEmployee[] }>(
    "/admin/presence-sources/opted-in",
  );
  return res.employees;
}
```

Acceptance: six named exports, all using the `api` client, no hardcoded fetch URLs.
  </action>
  <verify>
    <automated>grep -c "export async function" /Users/sebastianzabel/git/clokr/apps/web/src/lib/api/presence.ts | grep -q "^6$" && echo "PASS: 6 exports" || echo "FAIL: expected 6 exports"</automated>
  </verify>
  <done>File exists at `apps/web/src/lib/api/presence.ts` with exports: listSources, createSource, revokeSource, listDevices, mapDevice, listOptedInEmployees — all typed, all using `api` from `$api/client`.</done>
</task>

<task type="auto">
  <name>Task 2: Create admin WiFi-Presence page (three-block layout)</name>
  <files>apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte</files>
  <action>
Create `apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte`. This is a new route file — no parent directory exists yet, so create the directory implicitly via the file path.

The page implements the three-block layout from UI-SPEC.md. Use Svelte 5 runes (`$state`, `$derived`, `onMount`). All copy is from the UI-SPEC copywriting table. CSS uses only project CSS custom properties — no hardcoded hex.

**Script block:**

```typescript
import { onMount } from "svelte";
import Pagination from "$lib/components/ui/Pagination.svelte";
import {
  listSources,
  createSource,
  revokeSource,
  listDevices,
  mapDevice,
  listOptedInEmployees,
  type PresenceSource,
  type FritzDevice,
  type OptedInEmployee,
} from "$lib/api/presence";
import { api } from "$api/client";

// ── Block 1: PresenceSource keys ──────────────────────────────────────
let sources: PresenceSource[] = $state([]);
let sourcesLoading = $state(true);
let newSourceName = $state("");
let newSourceAdapterUrl = $state("");
let sourceCreating = $state(false);
let newRawKey = $state("");
let showNewKey = $state(false);
let srcPage = $state(1);
let srcPageSize = $state(10);
let pagedSources = $derived(sources.slice((srcPage - 1) * srcPageSize, srcPage * srcPageSize));

// ── Block 2: FritzBox devices ─────────────────────────────────────────
interface Employee { id: string; firstName: string; lastName: string; }
let employees: Employee[] = $state([]);
let selectedSourceId = $state("");
let devices: FritzDevice[] = $state([]);
let devicesLoading = $state(false);
let devicesError = $state("");
let devicesLastRefreshed = $state<Date | null>(null);
let deviceAssigning = $state<Record<string, boolean>>({}); // mac -> saving
let deviceAssignSelect = $state<Record<string, string>>({}); // mac -> selected employeeId
let devPage = $state(1);
let devPageSize = $state(10);
let pagedDevices = $derived(devices.slice((devPage - 1) * devPageSize, devPage * devPageSize));

// ── Block 3: Opt-in overview ──────────────────────────────────────────
let optedIn: OptedInEmployee[] = $state([]);
let optInLoading = $state(true);
let optPage = $state(1);
let optPageSize = $state(10);
let pagedOptedIn = $derived(optedIn.slice((optPage - 1) * optPageSize, optPage * optPageSize));

onMount(async () => {
  await loadSources();
  await loadOptedIn();
  // Load employees list for device assignment dropdown
  try {
    const res = await api.get<{ employees: Employee[] }>("/employees?pageSize=500");
    employees = res.employees ?? [];
  } catch { /* non-fatal */ }
  sourcesLoading = false;
  optInLoading = false;
});

async function loadSources() {
  try {
    sources = await listSources();
    if (sources.length > 0 && !selectedSourceId) {
      selectedSourceId = sources[0].id;
    }
  } catch { /* ignore — empty state shown */ }
}

async function loadOptedIn() {
  try {
    optedIn = await listOptedInEmployees();
  } catch { /* ignore */ }
}

async function handleCreateSource() {
  if (!newSourceName.trim()) return;
  sourceCreating = true;
  try {
    const res = await createSource(newSourceName.trim(), newSourceAdapterUrl.trim() || undefined);
    newRawKey = res.rawKey;
    showNewKey = true;
    newSourceName = "";
    newSourceAdapterUrl = "";
    await loadSources();
  } catch { /* error surfaced via showNewKey staying false */ }
  finally { sourceCreating = false; }
}

async function handleRevokeSource(id: string) {
  if (!confirm("Presence-Quelle widerrufen? Aktive Adapter können sich dann nicht mehr einloggen.")) return;
  try {
    await revokeSource(id);
    await loadSources();
  } catch { /* ignore */ }
}

function copyToClipboard(text: string) {
  navigator.clipboard.writeText(text);
}

async function refreshDevices() {
  if (!selectedSourceId) return;
  devicesLoading = true;
  devicesError = "";
  try {
    devices = await listDevices(selectedSourceId);
    devicesLastRefreshed = new Date();
    // Initialise per-device select state from current assignment
    const sel: Record<string, string> = {};
    for (const d of devices) {
      sel[d.mac] = d.assignedEmployeeId ?? "";
    }
    deviceAssignSelect = sel;
    devPage = 1;
  } catch {
    devicesError = "Geräteliste konnte nicht geladen werden. Bitte prüfen Sie, ob der Adapter erreichbar ist.";
  } finally {
    devicesLoading = false;
  }
}

async function saveAssignment(mac: string) {
  const employeeId = deviceAssignSelect[mac];
  if (!employeeId || !selectedSourceId) return;
  deviceAssigning = { ...deviceAssigning, [mac]: true };
  try {
    await mapDevice(selectedSourceId, mac, employeeId);
    // Update local device record
    devices = devices.map((d) =>
      d.mac === mac
        ? {
            ...d,
            assignedEmployeeId: employeeId,
            assignedEmployeeName: employees.find((e) => e.id === employeeId)
              ? `${employees.find((e) => e.id === employeeId)!.firstName} ${employees.find((e) => e.id === employeeId)!.lastName}`
              : employeeId,
          }
        : d,
    );
  } catch { /* ignore */ }
  finally { deviceAssigning = { ...deviceAssigning, [mac]: false }; }
}

function formatLastSeen(iso: string | null): string {
  if (!iso) return "—";
  return new Date(iso).toLocaleString("de-DE");
}
```

**Template block (all copy from UI-SPEC):**

```html
<svelte:head><title>WiFi-Presence – Clokr</title></svelte:head>

<div class="page-header card-animate">
  <h1 class="page-title">WiFi-Presence</h1>
  <p class="text-muted">FritzBox-Geräte verwalten und Mitarbeiter zuweisen</p>
</div>

<!-- ── Block 1: Presence-Quellen ──────────────────────────────────── -->
<div class="section-label card-animate">
  <h2 class="section-header">Presence-Quellen</h2>
  <p class="text-muted">API-Schlüssel für WiFi-Presence-Adapter verwalten. Jede Quelle benötigt einen eigenen Schlüssel.</p>
</div>

<div class="card card-body settings-card card-animate">
  {#if showNewKey}
    <div class="alert alert-success" role="status" aria-live="polite" style="margin: 1rem 0;">
      <div>
        <strong>Neuer Schlüssel erstellt!</strong>
        <p style="margin: 0.5rem 0;">Kopieren Sie den Schlüssel jetzt — er wird nicht erneut angezeigt:</p>
        <div style="display: flex; gap: 0.5rem; align-items: center;">
          <code style="flex: 1; padding: 0.5rem; background: var(--color-bg-subtle); border-radius: var(--radius-sm); word-break: break-all; font-size: 0.8125rem;">{newRawKey}</code>
          <button class="btn btn-sm btn-ghost" onclick={() => copyToClipboard(newRawKey)}>Schlüssel kopieren</button>
        </div>
        <button class="btn btn-sm btn-ghost" style="margin-top: 0.5rem;" onclick={() => { showNewKey = false; newRawKey = ""; }}>Schließen</button>
      </div>
    </div>
  {/if}

  <div style="display: flex; gap: 0.5rem; margin: 1rem 0; flex-wrap: wrap;">
    <input
      type="text"
      class="form-input"
      bind:value={newSourceName}
      placeholder="Quellen-Name (z.B. FritzBox Büro)"
      style="flex: 1; min-width: 200px;"
    />
    <input
      type="url"
      class="form-input"
      bind:value={newSourceAdapterUrl}
      placeholder="Adapter-URL (optional)"
      style="flex: 1; min-width: 180px;"
    />
    <button
      class="btn btn-primary"
      onclick={handleCreateSource}
      disabled={sourceCreating || !newSourceName.trim()}
    >
      Schlüssel erstellen
    </button>
  </div>

  {#if sourcesLoading}
    <div class="skeleton skeleton-text" style="height: 48px; border-radius: var(--radius-md);"></div>
  {:else if sources.length > 0}
    <div class="table-wrap">
      <table class="data-table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Schlüssel-Prefix</th>
            <th>Zuletzt verwendet</th>
            <th>Status</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {#each pagedSources as src (src.id)}
            <tr class:row-revoked={src.revokedAt}>
              <td>{src.name}</td>
              <td><code style="font-size: 0.8125rem;">{src.keyPrefix}</code></td>
              <td>{src.lastUsedAt ? new Date(src.lastUsedAt).toLocaleString("de-DE") : "Nie"}</td>
              <td>
                {#if src.revokedAt}
                  <span class="badge badge-red">Widerrufen</span>
                {:else}
                  <span class="badge badge-green">Aktiv</span>
                {/if}
              </td>
              <td>
                {#if !src.revokedAt}
                  <button
                    class="btn btn-sm btn-ghost"
                    style="color: var(--color-red);"
                    onclick={() => handleRevokeSource(src.id)}
                  >
                    Quelle widerrufen
                  </button>
                {/if}
              </td>
            </tr>
          {/each}
        </tbody>
      </table>
      <Pagination total={sources.length} bind:page={srcPage} bind:pageSize={srcPageSize} />
    </div>
  {:else}
    <p class="text-muted">Noch keine Presence-Quellen. Erstellen Sie einen Schlüssel für Ihren ersten Adapter.</p>
  {/if}
</div>

<!-- ── Block 2: FritzBox-Geräteliste ──────────────────────────────── -->
<div class="section-label card-animate">
  <h2 class="section-header">FritzBox-Geräteliste</h2>
  <p class="text-muted">Aktuell mit dem Netzwerk verbundene Geräte. MAC-Adressen können Mitarbeitern zugewiesen werden.</p>
</div>

<div class="card card-body settings-card card-animate">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; flex-wrap: wrap; gap: 0.5rem;">
    <div style="display: flex; align-items: center; gap: 1rem;">
      {#if sources.length > 1}
        <select class="form-input" bind:value={selectedSourceId} style="width: auto;">
          {#each sources.filter((s) => !s.revokedAt) as src (src.id)}
            <option value={src.id}>{src.name}</option>
          {/each}
        </select>
      {/if}
      <span class="text-muted" style="font-size: 0.8125rem;">
        {devicesLastRefreshed ? `Zuletzt aktualisiert: ${devicesLastRefreshed.toLocaleTimeString("de-DE")}` : "Noch nicht geladen"}
      </span>
    </div>
    <button
      class="btn btn-outline btn-sm"
      onclick={refreshDevices}
      disabled={devicesLoading || !selectedSourceId}
    >
      {devicesLoading ? "Aktualisieren…" : "Liste aktualisieren"}
    </button>
  </div>

  {#if devicesError}
    <div class="alert alert-error" role="alert">
      <span>⚠</span>
      <span>{devicesError}</span>
    </div>
  {:else if devicesLoading}
    <div class="skeleton skeleton-text" style="height: 120px; border-radius: var(--radius-md);"></div>
  {:else if devices.length > 0}
    <div class="table-wrap">
      <table class="data-table">
        <thead>
          <tr>
            <th>MAC-Adresse</th>
            <th>Hostname</th>
            <th>Zuletzt gesehen</th>
            <th>Status</th>
            <th>Mitarbeiter</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {#each pagedDevices as dev (dev.mac)}
            <tr>
              <td><code style="font-size: 0.8125rem; font-family: var(--font-mono);">{dev.mac}</code></td>
              <td>{dev.hostname || "—"}</td>
              <td style="font-size: 0.875rem;">{formatLastSeen(dev.lastSeen)}</td>
              <td>
                <span style="display: inline-flex; align-items: center; gap: 0.375rem;">
                  <span
                    style="display: inline-block; width: 8px; height: 8px; border-radius: 50%; background: {dev.online ? 'var(--color-green)' : 'var(--color-text-muted)'};"
                    aria-hidden="true"
                  ></span>
                  {dev.online ? "Online" : "Offline"}
                </span>
              </td>
              <td>
                {#if dev.assignedEmployeeId}
                  <span style="font-size: 0.875rem;">{dev.assignedEmployeeName ?? dev.assignedEmployeeId}</span>
                {:else}
                  <select
                    class="form-input"
                    bind:value={deviceAssignSelect[dev.mac]}
                    disabled={deviceAssigning[dev.mac]}
                    style="font-size: 0.875rem; padding: 0.25rem 0.5rem; min-width: 160px;"
                  >
                    <option value="">Mitarbeiter zuweisen…</option>
                    {#each employees as emp (emp.id)}
                      <option value={emp.id}>{emp.firstName} {emp.lastName}</option>
                    {/each}
                  </select>
                {/if}
              </td>
              <td>
                {#if !dev.assignedEmployeeId && deviceAssignSelect[dev.mac]}
                  <button
                    class="btn btn-sm btn-ghost"
                    onclick={() => saveAssignment(dev.mac)}
                    disabled={deviceAssigning[dev.mac]}
                  >
                    Zuweisung speichern
                  </button>
                {:else if dev.assignedEmployeeId}
                  <button
                    class="btn btn-sm btn-ghost"
                    onclick={() => {
                      devices = devices.map((d) =>
                        d.mac === dev.mac ? { ...d, assignedEmployeeId: null, assignedEmployeeName: null } : d
                      );
                      deviceAssignSelect = { ...deviceAssignSelect, [dev.mac]: "" };
                    }}
                  >
                    Bearbeiten
                  </button>
                {/if}
              </td>
            </tr>
          {/each}
        </tbody>
      </table>
      <Pagination total={devices.length} bind:page={devPage} bind:pageSize={devPageSize} />
    </div>
  {:else if devicesLastRefreshed}
    <p class="text-muted">Keine Geräte gefunden. Stellen Sie sicher, dass der FritzBox-Adapter läuft und verbunden ist.</p>
  {:else}
    <p class="text-muted">Klicken Sie auf "Liste aktualisieren", um die verbundenen Geräte zu laden.</p>
  {/if}
</div>

<!-- ── Block 3: Opt-in-Übersicht ──────────────────────────────────── -->
<div class="section-label card-animate">
  <h2 class="section-header">Opt-in-Übersicht</h2>
  <p class="text-muted">Welche Mitarbeiter haben WiFi-Präsenzerkennung aktiviert.</p>
</div>

<div class="card card-body settings-card card-animate">
  {#if optInLoading}
    <div class="skeleton skeleton-text" style="height: 48px; border-radius: var(--radius-md);"></div>
  {:else if optedIn.length > 0}
    <div class="table-wrap">
      <table class="data-table">
        <thead>
          <tr>
            <th>Mitarbeiter</th>
            <th>Opt-in</th>
            <th>MAC-Adressen</th>
            <th>Aktiviert am</th>
          </tr>
        </thead>
        <tbody>
          {#each pagedOptedIn as emp (emp.id)}
            <tr>
              <td>{emp.firstName} {emp.lastName}</td>
              <td>
                {#if emp.wifiPresenceEnabled}
                  <span class="badge badge-green">Aktiv</span>
                {:else}
                  <span class="badge badge-gray">Inaktiv</span>
                {/if}
              </td>
              <td style="font-size: 0.875rem;">
                {#if emp.wifiMacs.length === 0}
                  <span class="text-muted">—</span>
                {:else if emp.wifiMacs.length <= 2}
                  {#each emp.wifiMacs as mac (mac)}
                    <code style="font-size: 0.8125rem; margin-right: 0.25rem; font-family: var(--font-mono);">{mac}</code>
                  {/each}
                {:else}
                  <span class="badge badge-blue">{emp.wifiMacs.length} Geräte</span>
                {/if}
              </td>
              <td style="font-size: 0.875rem;">
                {emp.wifiOptInAt ? new Date(emp.wifiOptInAt).toLocaleDateString("de-DE") : "—"}
              </td>
            </tr>
          {/each}
        </tbody>
      </table>
      <Pagination total={optedIn.length} bind:page={optPage} bind:pageSize={optPageSize} />
    </div>
  {:else}
    <p class="text-muted">Kein Mitarbeiter hat WiFi-Präsenzerkennung aktiviert.</p>
  {/if}
</div>
```

**Style block — no hardcoded hex, only CSS custom properties:**

```css
<style>
  .page-header {
    margin-bottom: 2rem;
  }

  .page-title {
    font-size: 1.375rem;
    font-weight: 700;
    color: var(--color-text-heading);
    margin: 0 0 0.25rem;
  }

  .section-label {
    margin: 2rem 0 0.75rem;
  }

  .settings-card {
    background: var(--glass-bg);
    border: 1px solid var(--glass-border);
    box-shadow: var(--glass-shadow);
    backdrop-filter: blur(var(--glass-blur));
  }

  :global(.row-revoked) {
    opacity: 0.5;
  }
</style>
```

Run `pnpm --filter @clokr/web check` or `npx svelte-check` after writing — fix any type errors before completing.
  </action>
  <verify>
    <automated>grep -c "card-animate" /Users/sebastianzabel/git/clokr/apps/web/src/routes/\(app\)/admin/wifi-presence/+page.svelte | grep -qE "^[6-9]$|^[1-9][0-9]+" && echo "PASS: >=6 card-animate usages" || echo "CHECK: count card-animate occurrences manually"; grep -q "Presence-Quellen" /Users/sebastianzabel/git/clokr/apps/web/src/routes/\(app\)/admin/wifi-presence/+page.svelte && echo "PASS: copy present" && grep -q "FritzBox-Geräteliste" /Users/sebastianzabel/git/clokr/apps/web/src/routes/\(app\)/admin/wifi-presence/+page.svelte && echo "PASS: block 2 present" && grep -q "Opt-in-Übersicht" /Users/sebastianzabel/git/clokr/apps/web/src/routes/\(app\)/admin/wifi-presence/+page.svelte && echo "PASS: block 3 present"</automated>
  </verify>
  <done>
    - File exists at `apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte`
    - Page title is "WiFi-Presence – Clokr"
    - All three blocks present: "Presence-Quellen", "FritzBox-Geräteliste", "Opt-in-Übersicht"
    - Every primary content div has `card-animate`
    - New key reveal alert uses `aria-live="polite"`
    - Device status shows colored 8px dot + text ("Online"/"Offline") — no color-only indicator
    - No hardcoded hex values in style block
    - `svelte-check` reports no errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Add WiFi-Präsenz tab to admin navigation</name>
  <files>apps/web/src/routes/(app)/admin/+layout.svelte</files>
  <action>
Read the current `apps/web/src/routes/(app)/admin/+layout.svelte`. Locate the `tabs` array (line ~23-34). Add a new entry at the end of the array, before `.filter((t) => t.show)`:

```typescript
{ href: "/admin/wifi-presence", label: "WiFi-Präsenz", show: isAdmin },
```

This makes the tab visible only to ADMIN role (same as "Import" and "Audit Log" tabs). The `isAdmin` variable is already declared as `const isAdmin = $derived($authStore.user?.role === "ADMIN")` at line ~20.

No other changes to the layout file.
  </action>
  <verify>
    <automated>grep -q "wifi-presence" /Users/sebastianzabel/git/clokr/apps/web/src/routes/\(app\)/admin/+layout.svelte && echo "PASS: tab entry exists" || echo "FAIL: tab entry missing"</automated>
  </verify>
  <done>
    - `/admin/wifi-presence` entry exists in the `tabs` array in `+layout.svelte`
    - The entry uses `show: isAdmin` (ADMIN-only visibility)
    - Label is "WiFi-Präsenz"
    - No other lines in the layout were modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → clokr-api | Admin page calls authenticated API endpoints; JWT token required |
| admin-only endpoints | /admin/presence-sources/* restricted to ADMIN role by `requireRole('ADMIN')` on the API side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | Information Disclosure | Raw key reveal (`showNewKey` alert) | mitigate | Key displayed only once in-session via `showNewKey` boolean; "Schließen" clears `newRawKey = ""`; `aria-live="polite"` does not persist in DOM after close |
| T-25-06-02| Spoofing | Device assignment dropdown | accept | Employee list fetched from authenticated API; assignment POST is server-validated; no client-side trust |
| T-25-06-03 | Elevation of Privilege | WiFi-Präsenz tab visibility | mitigate | Tab uses `show: isAdmin` client-side AND `requireRole('ADMIN')` on every API endpoint — defense in depth |
| T-25-06-04 | Information Disclosure | Opt-in overview table (employee MAC addresses) | accept | Admin-only page; MACs are necessary for admin assignment workflow; consistent with Terminal key management precedent |
</threat_model>

<verification>
After plan execution:

1. Navigate to `/admin/wifi-presence` as ADMIN — page loads with all three blocks.
2. Create a PresenceSource: enter name, click "Schlüssel erstellen" — raw key alert appears with `aria-live`, copy button works, close button clears it.
3. Revoke a source: click "Quelle widerrufen" — browser confirm appears; on confirm, row shows `badge-red Widerrufen` and button disappears.
4. Click "Liste aktualisieren" with a valid source selected — device table populates (or error alert if adapter unreachable).
5. Device status column shows a colored dot + text ("Online" / "Offline").
6. Opt-in overview table shows opted-in employees with badge-green/badge-gray.
7. Log in as MANAGER — "WiFi-Präsenz" tab is NOT visible in admin nav.
8. Log in as ADMIN — "WiFi-Präsenz" tab IS visible.
9. `grep -c "var(--" apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte` — all color references use CSS custom properties (no hex).
</verification>

<success_criteria>
- `apps/web/src/routes/(app)/admin/wifi-presence/+page.svelte` exists and renders three blocks with correct German copy from UI-SPEC
- `apps/web/src/lib/api/presence.ts` exports exactly six named functions
- `/admin/wifi-presence` tab visible to ADMIN, hidden to MANAGER/EMPLOYEE
- All primary content divs have `card-animate` class
- No hardcoded hex colors in style blocks
- `svelte-check` passes with no type errors
- Raw key reveal uses `aria-live="polite"` on the alert container
- Device status uses colored dot + text (not color-only)
</success_criteria>

<output>
After completion, create `.planning/phases/25-wifi-presence-stempel-fritzbox/25-06-SUMMARY.md` using the summary template.
</output>
