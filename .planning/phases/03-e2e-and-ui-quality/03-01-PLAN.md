---
phase: 03-e2e-and-ui-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/routes/(app)/time-entries/+page.svelte
  - apps/e2e/tests/core-flows.spec.ts
  - apps/e2e/tests/time-entries-flow.spec.ts
autonomous: true
requirements: [E2E-01, E2E-02, UI-02]

must_haves:
  truths:
    - "Login -> Dashboard -> Clock-in flow asserts state change (Eingestempelt seit visible, clock-out button appears)"
    - "Time entry create flow fills form fields, clicks save, and asserts the entry appears in the calendar"
    - "Time entry edit flow modifies an existing entry and asserts the change persists after save"
    - "Time entry delete flow soft-deletes an entry and asserts it disappears from the UI"
    - "Attempting to edit/delete a locked-month entry shows 'Monat ist gesperrt' in the frontend"
    - "E2E assertion verifies the locked-month German error message is visible in the DOM"
  artifacts:
    - path: "apps/web/src/routes/(app)/time-entries/+page.svelte"
      provides: "Locked-month 403 error mapping to German message"
      contains: "Monat ist gesperrt"
    - path: "apps/e2e/tests/core-flows.spec.ts"
      provides: "E2E-01 clock-in/out flow with state assertions"
      contains: "Eingestempelt seit"
    - path: "apps/e2e/tests/time-entries-flow.spec.ts"
      provides: "E2E-02 CRUD flow + locked-month error assertion"
      contains: "Monat ist gesperrt"
  key_links:
    - from: "apps/web/src/routes/(app)/time-entries/+page.svelte"
      to: "/api/v1/time-entries"
      via: "api.put/api.delete catch blocks mapping 403 to German message"
      pattern: "status === 403"
    - from: "apps/e2e/tests/time-entries-flow.spec.ts"
      to: "apps/web/src/routes/(app)/time-entries/+page.svelte"
      via: "Playwright assertion on visible error text"
      pattern: "Monat ist gesperrt"
---

<objective>
Write substantive E2E tests for the login/clock-in/out flow (E2E-01) and time entry CRUD flow (E2E-02), and add the locked-month German error message to the frontend (UI-02) with an E2E assertion.

Purpose: Replace scaffold-only E2E specs with real state-change assertions, and surface the legally-required locked-month message to users attempting edits on closed months.

Output: Updated `core-flows.spec.ts` with clock-in/out assertions, updated `time-entries-flow.spec.ts` with full CRUD + locked-month tests, updated `+page.svelte` with 403-to-German-message mapping.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-e2e-and-ui-quality/03-CONTEXT.md
@.planning/phases/03-e2e-and-ui-quality/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/e2e/tests/helpers.ts:
```typescript
export const TEST_ADMIN = {
  email: process.env.TEST_ADMIN_EMAIL || "admin@clokr.de",
  password: process.env.TEST_ADMIN_PASSWORD || "Admin123!Pass",
};
export async function loginAsAdmin(page: Page): Promise<void>;
export async function screenshotPage(page: Page, name: string): Promise<void>;
```

From apps/web/src/lib/api/client.ts:
```typescript
// ApiError is NOT exported. Use duck-typing: (e as any)?.status === 403
class ApiError extends Error {
  constructor(public status: number, message: string, public data?: unknown) {}
}
```

Clock-in button locators (from dashboard/+page.svelte):
- Before clock-in: `.clock-btn--in` (text: "Einstempeln")
- After clock-in: `.clock-btn--out` (text: "Ausstempeln")
- Status text after clock-in: "Eingestempelt seit HH:mm Uhr"

Time entry error handling (from time-entries/+page.svelte):
- `saveEntry()` catch at line ~609: `saveError = e instanceof Error ? e.message : "Fehler beim Speichern"`
- `deleteEntry()` catch at line ~621: `error = e instanceof Error ? e.message : "Fehler beim Löschen"`
- `saveError` displayed in `.alert.alert-error` inside modal (line ~1164)

API locked-month responses:
- PUT returns 403 `{ error: "Eintrag ist gesperrt und kann nicht bearbeitet werden" }`
- DELETE returns 403 `{ error: "Eintrag ist gesperrt und kann nicht gelöscht werden" }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add locked-month German error message to time entries page</name>
  <read_first>
    - apps/web/src/routes/(app)/time-entries/+page.svelte
    - apps/web/src/lib/api/client.ts
  </read_first>
  <files>apps/web/src/routes/(app)/time-entries/+page.svelte</files>
  <action>
    Modify two catch blocks in `apps/web/src/routes/(app)/time-entries/+page.svelte` to detect HTTP 403 and show "Monat ist gesperrt". Per D-08.

    1. In `saveEntry()` catch block (around line 609), change from:
    ```typescript
    } catch (e: unknown) {
      saveError = e instanceof Error ? e.message : "Fehler beim Speichern";
    }
    ```
    To:
    ```typescript
    } catch (e: unknown) {
      if ((e as any)?.status === 403) {
        saveError = "Monat ist gesperrt";
      } else {
        saveError = e instanceof Error ? e.message : "Fehler beim Speichern";
      }
    }
    ```

    2. In `deleteEntry()` catch block (around line 621), change from:
    ```typescript
    } catch (e: unknown) {
      error = e instanceof Error ? e.message : "Fehler beim Löschen";
    }
    ```
    To:
    ```typescript
    } catch (e: unknown) {
      if ((e as any)?.status === 403) {
        error = "Monat ist gesperrt";
      } else {
        error = e instanceof Error ? e.message : "Fehler beim Löschen";
      }
    }
    ```

    ApiError is NOT exported from client.ts, so use duck-typing `(e as any)?.status === 403`. Do NOT modify any other catch blocks (e.g., revalidateEntry). Per D-10 all other German error messages are confirmed present.
  </action>
  <verify>
    <automated>grep -c "Monat ist gesperrt" apps/web/src/routes/\(app\)/time-entries/+page.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Monat ist gesperrt" apps/web/src/routes/\(app\)/time-entries/+page.svelte` outputs `2`
    - `grep "status === 403" apps/web/src/routes/\(app\)/time-entries/+page.svelte | wc -l` outputs `2`
    - The saveEntry() function still has `"Fehler beim Speichern"` in its else branch
    - The deleteEntry() function still has `"Fehler beim Löschen"` in its else branch
    - The revalidateEntry() catch block is unchanged (still uses "Fehler beim Revalidieren")
  </acceptance_criteria>
  <done>Both saveEntry() and deleteEntry() catch blocks map HTTP 403 to "Monat ist gesperrt", non-403 errors fall through to existing German messages.</done>
</task>

<task type="auto">
  <name>Task 2: Write substantive E2E tests for clock-in/out, time entry CRUD, and locked-month assertion</name>
  <read_first>
    - apps/e2e/tests/core-flows.spec.ts
    - apps/e2e/tests/time-entries-flow.spec.ts
    - apps/e2e/tests/helpers.ts
    - apps/e2e/playwright.config.ts
    - apps/web/src/routes/(app)/dashboard/+page.svelte (lines 640-680, clock button markup)
    - apps/web/src/routes/(app)/time-entries/+page.svelte (lines 1090-1270, modal and calendar markup)
  </read_first>
  <files>apps/e2e/tests/core-flows.spec.ts, apps/e2e/tests/time-entries-flow.spec.ts</files>
  <action>
    Per D-01, write substantive test bodies in EXISTING files. Per D-02, use existing storageState admin auth.

    **core-flows.spec.ts (E2E-01)**

    Keep the existing "navigate through all main pages without errors" and "navigate admin pages without errors" tests unchanged. Replace the "command palette opens with Ctrl+K" and "theme switcher works" tests with two substantive clock tests:

    Test "clock in and verify running state":
    1. `await page.goto("/dashboard"); await page.waitForLoadState("networkidle");`
    2. Reset state: if `.clock-btn--out` is visible, click it and wait for `.clock-btn--in` to appear (timeout 10s)
    3. `const clockInBtn = page.locator(".clock-btn--in"); await expect(clockInBtn).toBeVisible();`
    4. `await clockInBtn.click();`
    5. `await expect(page.locator(".clock-btn--out")).toBeVisible({ timeout: 10_000 });`
    6. `await expect(page.getByText(/Eingestempelt seit/)).toBeVisible();`
    7. `await screenshotPage(page, "flow-clock-in-active");`

    Test "clock out and verify stopped state":
    1. `await page.goto("/dashboard"); await page.waitForLoadState("networkidle");`
    2. Ensure clocked in: if `.clock-btn--in` visible, click it and wait for `.clock-btn--out` (timeout 10s)
    3. `const clockOutBtn = page.locator(".clock-btn--out"); await expect(clockOutBtn).toBeVisible();`
    4. `await clockOutBtn.click();`
    5. `await expect(page.locator(".clock-btn--in")).toBeVisible({ timeout: 10_000 });`
    6. `await expect(page.getByText(/Eingestempelt seit/)).not.toBeVisible();`
    7. `await screenshotPage(page, "flow-clock-out-stopped");`

    **time-entries-flow.spec.ts (E2E-02 + UI-02)**

    Replace the file body. Keep the same describe wrapper "Zeiterfassung - Complete Flow" and beforeEach (loginAsAdmin, goto /time-entries, waitForLoadState).

    Keep test "page loads with calendar and summary bar" as-is.

    Test "create a manual time entry":
    1. Click `page.getByText(/Eintrag hinzufügen/i)`
    2. `const modal = page.locator("[role='dialog'], .modal").first(); await expect(modal).toBeVisible();`
    3. Compute a weekday date 7 days ago: `const d = new Date(); d.setDate(d.getDate() - 7); while (d.getDay() === 0 || d.getDay() === 6) d.setDate(d.getDate() - 1); const dateStr = d.toISOString().split("T")[0];`
    4. Fill date: `await modal.locator("input[type='date']").first().fill(dateStr);`
    5. Fill start: `await modal.locator("input[type='time']").first().fill("08:00");`
    6. Fill end: `await modal.locator("input[type='time']").nth(1).fill("16:30");`
    7. Click save: `await modal.getByRole("button", { name: /speichern|erstellen/i }).first().click();`
    8. Assert modal closes: `await expect(modal).not.toBeVisible({ timeout: 5_000 });`
    9. `await screenshotPage(page, "flow-time-entry-created");`

    Test "edit an existing time entry":
    1. Navigate the calendar to the week containing the date from the create test (the current month view should already show it if it was 7 days ago)
    2. Find and click on the calendar day cell that has the entry (the day cell for the created date should have hours displayed). Use the date string from the create test to locate: `page.locator(\`.cal-day[data-date="${dateStr}"]\`)` or similar. If no data-date attribute, find the day by its number text.
    3. Wait for modal: `await expect(modal).toBeVisible();`
    4. Change end time to "17:00"
    5. Click save, assert modal closes
    6. `await screenshotPage(page, "flow-time-entry-edited");`

    Test "delete a time entry":
    1. Click on the same day cell to open the entry
    2. Find the delete button (look for a button with trash icon, or `page.getByRole("button", { name: /löschen/i })`)
    3. Confirm deletion: click "Ja" in the confirmation prompt
    4. Assert modal/detail closes
    5. `await screenshotPage(page, "flow-time-entry-deleted");`

    Test "locked-month edit shows German error message" (per D-09):
    1. Use `page.route()` to intercept PUT requests to time-entries API and return a mocked 403:
       ```typescript
       await page.route("**/api/v1/time-entries/**", async (route) => {
         if (route.request().method() === "PUT") {
           await route.fulfill({
             status: 403,
             contentType: "application/json",
             body: JSON.stringify({ error: "Eintrag ist gesperrt und kann nicht bearbeitet werden" }),
           });
         } else {
           await route.continue();
         }
       });
       ```
    2. Click "Eintrag hinzufügen" to open the modal
    3. Fill a date, start time "09:00", end time "17:00"
    4. Click save
    5. `await expect(page.getByText("Monat ist gesperrt")).toBeVisible();`
    6. `await screenshotPage(page, "flow-locked-month-error");`
    7. Clean up: `await page.unroute("**/api/v1/time-entries/**");`

    **Patterns:**
    - Always `await expect(element).toBeVisible()` -- never optional `if (await el.isVisible())`
    - Use `page.waitForLoadState("networkidle")` -- never `page.waitForTimeout()` for async waits
    - Keep `loginAsAdmin(page)` in beforeEach
    - Use `screenshotPage()` after each test
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "await expect" apps/e2e/tests/core-flows.spec.ts && grep -c "await expect" apps/e2e/tests/time-entries-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Eingestempelt seit" apps/e2e/tests/core-flows.spec.ts` outputs at least `1`
    - `grep -c "clock-btn--in" apps/e2e/tests/core-flows.spec.ts` outputs at least `2`
    - `grep -c "clock-btn--out" apps/e2e/tests/core-flows.spec.ts` outputs at least `2`
    - `grep -c "Monat ist gesperrt" apps/e2e/tests/time-entries-flow.spec.ts` outputs at least `1`
    - `grep -c "page.route" apps/e2e/tests/time-entries-flow.spec.ts` outputs at least `1`
    - `grep -c "await expect" apps/e2e/tests/core-flows.spec.ts` outputs at least `6`
    - `grep -c "await expect" apps/e2e/tests/time-entries-flow.spec.ts` outputs at least `8`
    - No `if (await` optional assertion pattern remains in either file
    - Both files still import from `"./helpers"` and use `loginAsAdmin`
  </acceptance_criteria>
  <done>core-flows.spec.ts has clock-in/out tests with hard state assertions. time-entries-flow.spec.ts has CRUD tests plus locked-month error assertion. All use hard expect(), no optional if-visible patterns.</done>
</task>

</tasks>

<verification>
- `grep -c "Monat ist gesperrt" apps/web/src/routes/\(app\)/time-entries/+page.svelte` outputs `2`
- `grep -c "Eingestempelt seit" apps/e2e/tests/core-flows.spec.ts` outputs at least `1`
- `grep -c "Monat ist gesperrt" apps/e2e/tests/time-entries-flow.spec.ts` outputs at least `1`
- `grep -rn "if (await.*isVisible" apps/e2e/tests/core-flows.spec.ts apps/e2e/tests/time-entries-flow.spec.ts` outputs no matches (all optional assertions removed)
</verification>

<success_criteria>
1. The time entries page maps HTTP 403 to "Monat ist gesperrt" in both save and delete handlers
2. core-flows.spec.ts contains substantive clock-in and clock-out tests with state change assertions (not just page navigation)
3. time-entries-flow.spec.ts contains create, edit, delete, and locked-month error tests with hard assertions
4. No `if (await el.isVisible())` optional assertion patterns remain in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-and-ui-quality/03-01-SUMMARY.md`
</output>
