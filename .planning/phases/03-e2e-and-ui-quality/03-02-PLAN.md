---
phase: 03-e2e-and-ui-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/leave-flow.spec.ts
  - apps/e2e/tests/admin-settings-flow.spec.ts
autonomous: true
requirements: [E2E-03, E2E-04, E2E-05, UI-05]

must_haves:
  truths:
    - "Leave request creation submits successfully and the request appears in the Meine Antraege list"
    - "Leave request approval by admin changes the request status to APPROVED in the UI"
    - "Admin can create a new employee via the UI form and the employee appears in the employee table"
    - "Monatsabschluss close-month action triggers the API call and the UI reflects the closed status"
    - "Password policy settings can be changed and saved, and the changes persist after page reload"
  artifacts:
    - path: "apps/e2e/tests/leave-flow.spec.ts"
      provides: "E2E-03 leave request and approval flow"
      contains: "APPROVED"
    - path: "apps/e2e/tests/admin-settings-flow.spec.ts"
      provides: "E2E-04 employee management, E2E-05 Monatsabschluss, UI-05 password policy"
      contains: "Monatsabschluss"
  key_links:
    - from: "apps/e2e/tests/leave-flow.spec.ts"
      to: "/api/v1/leave"
      via: "page.request.post/put for leave creation and approval"
      pattern: "api/v1/leave"
    - from: "apps/e2e/tests/admin-settings-flow.spec.ts"
      to: "/api/v1/overtime/close-month"
      via: "UI button click triggering close-month API"
      pattern: "close-month"
    - from: "apps/e2e/tests/admin-settings-flow.spec.ts"
      to: "/api/v1/settings/security"
      via: "Password policy save calls PUT endpoint"
      pattern: "settings/security"
---

<objective>
Write substantive E2E tests for the leave approval flow (E2E-03), admin employee management (E2E-04), Monatsabschluss lock/unlock (E2E-05), and password policy persistence (UI-05).

Purpose: Replace scaffold tests that only navigate to pages with real tests that exercise state changes, API interactions, and persistence.

Output: Updated `leave-flow.spec.ts` with leave creation and approval assertions, updated `admin-settings-flow.spec.ts` with employee creation, Monatsabschluss close, and password policy save tests.
</objective>

<execution_context>
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastianzabel/git/clokr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-e2e-and-ui-quality/03-CONTEXT.md
@.planning/phases/03-e2e-and-ui-quality/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/e2e/tests/helpers.ts:
```typescript
export const TEST_ADMIN = {
  email: process.env.TEST_ADMIN_EMAIL || "admin@clokr.de",
  password: process.env.TEST_ADMIN_PASSWORD || "Admin123!Pass",
};
export async function loginAsAdmin(page: Page): Promise<void>;
export async function screenshotPage(page: Page, name: string): Promise<void>;
```

Seeded users (from packages/db/src/seed.ts):
- admin@clokr.de (ADMIN role) - password: Admin123!Pass
- max@clokr.de (EMPLOYEE role) - password: from seed (check seed.ts)

Leave API:
- POST /api/v1/leave - create leave request (body: { employeeId, type, startDate, endDate, ... })
- PUT /api/v1/leave/:id - update leave request
- PUT /api/v1/leave/:id/review - approve/reject (body: { action: "APPROVED" | "REJECTED" })
- Self-approval blocked: "Eigene Antraege koennen nicht selbst genehmigt werden" (400)

Employee API:
- POST /api/v1/employees - create employee
- GET /api/v1/employees - list employees

Monatsabschluss:
- POST /api/v1/overtime/close-month - { employeeId, year, month }
- GET /api/v1/overtime/close-month/year-status?year=YYYY - returns months[].status
- Frontend closeMonth() iterates readyEmployees, calls POST per employee
- UI success: "abgeschlossen" text appears, month badge changes

Password policy:
- PUT /api/v1/settings/security - { passwordMinLength, passwordRequireUpper, ... }
- GET /api/v1/settings/security - returns current settings
- Frontend: savePasswordPolicy() in admin/system/+page.svelte (line ~462)
- UI fields: pwMinLength input, pwRequireUpper/Special toggle switches
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write substantive leave approval E2E flow</name>
  <read_first>
    - apps/e2e/tests/leave-flow.spec.ts
    - apps/e2e/tests/helpers.ts
    - apps/web/src/routes/(app)/leave/+page.svelte (form structure, tab names, approval UI)
    - apps/api/src/routes/leave.ts (self-approval check at line ~505, review endpoint)
    - packages/db/src/seed.ts (seeded employee data, max@clokr.de password)
  </read_first>
  <files>apps/e2e/tests/leave-flow.spec.ts</files>
  <action>
    Per D-01, write substantive test bodies in the EXISTING file. Per D-03, exercise the employee-creates -> manager-approves path. Research confirmed admin can act as approver for requests created by other employees.

    Replace the file body with substantive tests. Keep the describe wrapper "Abwesenheiten - Complete Flow" and beforeEach (loginAsAdmin, goto /leave, waitForLoadState).

    **Strategy for leave approval (E2E-03) avoiding self-approval block:**

    The admin user cannot approve their own leave request. To test the approval flow:
    1. Use `page.request` (which inherits the admin's authenticated browser context) to create a leave request via API on behalf of the seeded employee (max@clokr.de). The admin has permission to create leave for any employee.
    2. First, get the employee ID: `const empRes = await page.request.get("/api/v1/employees"); const employees = await empRes.json(); const maxEmployee = employees.find((e: any) => e.user?.email === "max@clokr.de") || employees[1];`
    3. Create a leave request via API: `const leaveRes = await page.request.post("/api/v1/leave", { data: { employeeId: maxEmployee.id, type: "VACATION", startDate: futureDate, endDate: futureDate, note: "E2E test" } });`
    4. If the POST succeeds, capture the leave ID from the response.
    5. Approve it via API: `await page.request.put(\`/api/v1/leave/${leaveId}/review\`, { data: { action: "APPROVED" } });`
    6. Then navigate to the Genehmigungen tab in the UI and assert the approved leave is visible.
    7. If the API-based creation fails (e.g., entitlement issues), fall back to creating via the UI form with a SICK leave type (no entitlement constraints).

    **Test structure:**

    Test "page loads with tabs and vacation summary" - keep as-is (uses hard expects already).

    Test "create leave request via UI form":
    1. Click "Neuer Antrag"
    2. `await expect(page.locator("#f-type").first()).toBeVisible();` (hard assert, not optional)
    3. Select leave type: `await page.locator("#f-type").first().selectOption("SICK");` (SICK avoids entitlement issues)
    4. Set start date to 14 days from now (weekday): compute and fill `#f-start`
    5. Set end date to same date: fill `#f-end`
    6. Click submit: `await page.getByRole("button", { name: /einreichen|antrag stellen/i }).first().click();`
    7. Wait for success: `await page.waitForLoadState("networkidle");`
    8. Assert: navigate to "Meine Antraege" tab, verify the request appears
    9. `await screenshotPage(page, "flow-leave-created");`

    Test "approve leave request (employee-creates, admin-approves)":
    1. Get employee list via `page.request.get("/api/v1/employees")`
    2. Find the seeded employee (max@clokr.de or the second employee)
    3. Compute a future weekday date (30 days out)
    4. Create a SICK leave request for that employee via `page.request.post("/api/v1/leave", { data: { employeeId, type: "SICK", startDate, endDate } })`
    5. Extract the leave ID from the response
    6. Navigate to the leave page and click the "Genehmigungen" tab
    7. Find the pending request in the approvals list
    8. Click the approve button (look for a button with checkmark or "Genehmigen" text)
    9. Assert the status changes to approved (text "Genehmigt" or "APPROVED" visible)
    10. Alternatively, verify via API: `const statusRes = await page.request.get(\`/api/v1/leave/${leaveId}\`); expect((await statusRes.json()).status).toBe("APPROVED");`
    11. `await screenshotPage(page, "flow-leave-approved");`

    Test "special leave shows reason dropdown" - keep existing test but convert optional assertions to hard assertions.

    **Patterns:**
    - All `if (await el.isVisible())` -> `await expect(el).toBeVisible()`
    - Use `page.request` for API calls within E2E tests (inherits browser context auth)
    - Use `screenshotPage()` after each test
    - Use SICK leave type to avoid vacation entitlement constraints in test data
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "await expect" apps/e2e/tests/leave-flow.spec.ts && grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await expect" apps/e2e/tests/leave-flow.spec.ts` outputs at least `6`
    - `grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts` outputs at least `2` (API calls for employee lookup and leave creation)
    - `grep "APPROVED\|Genehmigt\|genehmig" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
    - No `if (await` optional assertion pattern remains in the file
    - File still imports `loginAsAdmin` and `screenshotPage` from `"./helpers"`
  </acceptance_criteria>
  <done>leave-flow.spec.ts has substantive leave creation and approval tests with hard assertions. The approval flow exercises the employee-creates -> admin-approves path without triggering self-approval blocks.</done>
</task>

<task type="auto">
  <name>Task 2: Write substantive admin employee, Monatsabschluss, and password policy E2E tests</name>
  <read_first>
    - apps/e2e/tests/admin-settings-flow.spec.ts
    - apps/e2e/tests/helpers.ts
    - apps/web/src/routes/(app)/admin/employees/+page.svelte (employee form fields, table structure)
    - apps/web/src/routes/(app)/admin/monatsabschluss/+page.svelte (closeMonth function, status display, lines 162-213)
    - apps/web/src/routes/(app)/admin/system/+page.svelte (savePasswordPolicy function, security section, lines 462-481)
    - apps/api/src/routes/settings.ts (GET/PUT /settings/security endpoint, Zod schema)
  </read_first>
  <files>apps/e2e/tests/admin-settings-flow.spec.ts</files>
  <action>
    Per D-01, write substantive test bodies in the EXISTING file. Per D-04, Monatsabschluss must assert lock status. Per D-11/D-12, password policy UI is already built -- only E2E test needed.

    Keep the describe wrapper "Admin Settings - Complete Flow" and beforeEach (loginAsAdmin).

    Keep existing tests that already have value: "navigate all admin tabs", "admin special-leave - view rules table", "admin special-leave - open create modal". Convert any `if (await el.isVisible())` optional patterns to hard `await expect(el).toBeVisible()`.

    **Add/replace these tests:**

    Test "admin employees - create new employee" (E2E-04):
    1. `await page.goto("/admin/employees"); await page.waitForLoadState("networkidle");`
    2. Click "Mitarbeiter anlegen": `await page.getByText(/Mitarbeiter anlegen/i).first().click();`
    3. Assert modal opens: `const modal = page.locator("[role='dialog'], .modal").first(); await expect(modal).toBeVisible();`
    4. Fill the form fields with unique test data:
       - firstName: `await modal.getByLabel(/Vorname/i).fill("E2E");`
       - lastName: `await modal.getByLabel(/Nachname/i).fill("Testmitarbeiter");`
       - email: `await modal.getByLabel(/E-Mail/i).fill(\`e2e-${Date.now()}@test.de\`);`
       - employeeNumber: `await modal.getByLabel(/Personalnummer/i).fill(\`E2E-${Date.now()}\`);`
       - If a hire date field exists: fill with today's date
    5. Click save/create: `await modal.getByRole("button", { name: /anlegen|erstellen|speichern/i }).first().click();`
    6. Wait for response: `await page.waitForLoadState("networkidle");`
    7. Assert modal closes and employee appears in table: `await expect(page.getByText("E2E Testmitarbeiter").first()).toBeVisible({ timeout: 5_000 });`
    8. `await screenshotPage(page, "flow-admin-employee-created");`

    Test "admin monatsabschluss - view month status and attempt close" (E2E-05, per D-04):
    1. `await page.goto("/admin/monatsabschluss"); await page.waitForLoadState("networkidle");`
    2. Assert page title: `await expect(page.getByText(/Monatsabschluss/).first()).toBeVisible();`
    3. Assert months are listed: `await expect(page.getByText(/Januar|Februar|März/).first()).toBeVisible();`
    4. Check year-status via API to understand current state: `const statusRes = await page.request.get("/api/v1/overtime/close-month/year-status?year=2026");`
    5. Find a month that can be closed (status "open" or "ready"), or verify already-closed month shows correct status
    6. If a closeable month exists, click the close button for that month and assert status changes
    7. If no closeable month exists (no ready employees), assert the UI correctly shows the current status (e.g., "offen", "abgeschlossen" badges are displayed)
    8. Alternative robust approach: verify via API that the year-status endpoint returns valid data, then assert the UI displays matching status text for at least one month. Example: `const data = await statusRes.json(); const closedMonth = data.months?.find((m: any) => m.status === "closed"); if (closedMonth) { await expect(page.getByText(/abgeschlossen/i).first()).toBeVisible(); }`
    9. `await screenshotPage(page, "flow-admin-monatsabschluss-status");`

    Test "admin system - save password policy and verify persistence" (UI-05, per D-11):
    1. `await page.goto("/admin/system"); await page.waitForLoadState("networkidle");`
    2. Scroll to the "Passwort-Richtlinie" section: `await page.getByText("Passwort-Richtlinie").scrollIntoViewIfNeeded();`
    3. Read the current min-length value: `const minLengthInput = page.locator("input[type='number']").filter({ has: page.locator("..").filter({ hasText: /Mindestlänge|mindestlänge/i }) }).first();` or find it by nearby label. Alternative: use `page.locator("#pw-min-length")` if the input has an ID.
    4. Get the current value, change it: store current value, clear input, type a new value (e.g., if current is 8, change to 12)
    5. Find and click the save button for the password policy section: `await page.getByRole("button", { name: /speichern/i }).filter({ has: page.locator("..").filter({ hasText: /Passwort/i }) }).first().click();` or more directly find the save button near the password policy section
    6. Wait for save: `await page.waitForLoadState("networkidle");`
    7. Reload the page to verify persistence: `await page.reload(); await page.waitForLoadState("networkidle");`
    8. Scroll to the password policy section again
    9. Assert the saved value persists: read the min-length input value and assert it equals the value set in step 4
    10. Restore the original value: change back to original, click save
    11. `await screenshotPage(page, "flow-admin-password-policy-saved");`

    **Patterns:**
    - All `if (await el.isVisible())` -> `await expect(el).toBeVisible()`
    - Use `page.request` for API verification calls
    - Use `screenshotPage()` after each test
    - Unique test data (Date.now() suffix) to avoid collisions
  </action>
  <verify>
    <automated>cd /Users/sebastianzabel/git/clokr && grep -c "await expect" apps/e2e/tests/admin-settings-flow.spec.ts && grep -c "Mitarbeiter anlegen" apps/e2e/tests/admin-settings-flow.spec.ts && grep -c "Passwort" apps/e2e/tests/admin-settings-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await expect" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `10`
    - `grep "E2E.*Testmitarbeiter\|e2e-.*@test.de" apps/e2e/tests/admin-settings-flow.spec.ts | wc -l` outputs at least `1` (employee creation test data)
    - `grep -c "close-month\|Monatsabschluss\|abgeschlossen" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `2`
    - `grep -c "Passwort-Richtlinie\|password.*policy\|settings/security" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
    - `grep -c "page.reload" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1` (persistence verification)
    - No `if (await` optional assertion pattern remains in the file
    - File still imports `loginAsAdmin` and `screenshotPage` from `"./helpers"`
  </acceptance_criteria>
  <done>admin-settings-flow.spec.ts has substantive employee creation (E2E-04), Monatsabschluss status verification (E2E-05), and password policy save+persistence (UI-05) tests. All use hard assertions.</done>
</task>

</tasks>

<verification>
- `grep -c "page.request" apps/e2e/tests/leave-flow.spec.ts` outputs at least `2`
- `grep "APPROVED\|Genehmigt" apps/e2e/tests/leave-flow.spec.ts | wc -l` outputs at least `1`
- `grep -c "E2E" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
- `grep -c "page.reload" apps/e2e/tests/admin-settings-flow.spec.ts` outputs at least `1`
- `grep -rn "if (await.*isVisible" apps/e2e/tests/leave-flow.spec.ts apps/e2e/tests/admin-settings-flow.spec.ts` outputs no matches
</verification>

<success_criteria>
1. leave-flow.spec.ts contains substantive leave creation and approval flow tests with hard assertions (E2E-03)
2. admin-settings-flow.spec.ts contains employee creation test with form fill and table assertion (E2E-04)
3. admin-settings-flow.spec.ts contains Monatsabschluss status verification test (E2E-05, per D-04)
4. admin-settings-flow.spec.ts contains password policy save and persistence test with page reload (UI-05, per D-11)
5. No `if (await el.isVisible())` optional assertion patterns remain in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-and-ui-quality/03-02-SUMMARY.md`
</output>
